CHANGE LOG (continued from overviewProject.txt, closed at 1000 lines)
--------------------------------------------------------
Full project overview (stack, file map, account types, spec build
sequence, etc.) stays authoritative in overviewProject.txt. This file
is change-log entries only, newest at the bottom, continuing where
that file left off.

[2026-09-02] Added a "beating heart" hover pulse to the homepage's
  card-style outlines: new shared @keyframes cardHeartbeatPulse in
  app/styles/shared.css (two quick box-shadow pulses then a rest,
  mimicking an actual heartbeat rhythm; respects prefers-reduced-
  motion). Wired into :hover on .whyUsItem (shared.css) and
  .categoryCard/.featuredProductCard/.testimonialHomeCard/.statCard
  (home.css) alongside each card's existing border-color/transform
  hover styles — .categoryCard also feeds its own --categoryAccent
  into the pulse color via --cardHeartbeatColor so each category
  keeps its accent tint.

[2026-09-02] Fixed the Create Account tab getting clipped on
  /auth/login (app/styles/auth.css): .authGlassCard had no height cap,
  and its parent .authPageWrapper is overflow: hidden with no page
  scroll (full-bleed background slideshow), so the longer Register
  form (name/email/password/confirm/terms) could grow taller than the
  viewport with the bottom of the form — including the submit button —
  clipped and unreachable. Added max-height: calc(100dvh - 2rem) +
  overflow-y: auto to .authGlassCard so it scrolls internally instead.

[2026-09-03] Started visitor_specification.md Implementation Order
  Step 2 (category grids): confirmed "/shop" is a retired redirect to
  "/pricing" (no grid to animate there, spec note is stale). Fixed
  .productCard hover in app/styles/shared.css — was still box-shadow-
  only despite the spec claiming it was already switched; now uses
  transform: translateY(-4px) + border/background brighten per Rule
  17.7 (shadows reserved for floating elements). This is a shared
  class so it applies to every product grid site-wide. Wired the
  ScrollReveal scroll-entrance stagger (same 0.06s step, 8-card cap as
  ProductsGrid.tsx) into components/home/TShirtsSection.tsx's product
  grid, which powers both the homepage T-Shirts section and the
  "/tshirts" category page. Remaining category grids (templates,
  ai-videos, file-tools, game-characters, tutorials) still pending.

[2026-09-03] Continued visitor_specification.md Implementation Order
  Step 2: wired the ScrollReveal scroll-entrance stagger (0.06s step,
  8-card cap) into components/home/TemplatesSection.tsx's
  productCardsGrid, which powers both the homepage Templates section
  and the "/templates" category page. Remaining category grids
  (ai-videos, file-tools, game-characters, tutorials) still pending.

[2026-09-03] Continued visitor_specification.md Implementation Order
  Step 2: wired the ScrollReveal scroll-entrance stagger (0.06s step,
  8-card cap) into components/home/AIVideosSection.tsx's
  productCardsGrid, which powers both the homepage AI Videos section
  and the "/ai-videos" category page. Noted for follow-up:
  GameCharactersSection.tsx and FileToolsSection.tsx have no entrance
  animation on their product grids yet; TutorialsSection.tsx has its
  own hand-rolled stagger that should eventually be normalized to
  ScrollReveal (currently skips the prefers-reduced-motion check).
  Remaining: file-tools, game-characters, tutorials.

[2026-09-03] Continued visitor_specification.md Implementation Order
  Step 2: wired the ScrollReveal scroll-entrance stagger (0.06s step,
  8-card cap) into components/home/FileToolsSection.tsx's
  productCardsGrid, which powers both the homepage File Tools section
  and the "/file-tools" category page. This grid previously had no
  entrance animation at all. Remaining: game-characters, tutorials.

[2026-09-03] Continued visitor_specification.md Implementation Order
  Step 2: wired the ScrollReveal scroll-entrance stagger (0.06s step,
  8-card cap) into components/home/GameCharactersSection.tsx's
  productCardsGrid (bottom product-card row with Rigged/Modular/
  Low-poly tags) — each gameCharacterCardWrap (badge + ProductCard)
  is now the ScrollReveal child so badge and card animate together.
  The character-thumbnail gallery above already had its own stagger/
  scale entrance and was left untouched. Remaining: tutorials.

[2026-09-03] Completed visitor_specification.md Implementation Order
  Step 2: normalized components/home/TutorialsSection.tsx's product
  grid from a hand-rolled motion.div stagger (raw initial/whileInView/
  transition, no reduced-motion handling) to the shared ScrollReveal
  primitive, preserving the same per-level-group delay formula
  (groupIndex * 0.15 + itemIndex * 0.05). All six category grids
  (tshirts, templates, ai-videos, file-tools, game-characters,
  tutorials) plus /products now use ScrollReveal consistently; /shop
  confirmed as a retired redirect with nothing to animate. Next step
  in the spec's Implementation Order: /pricing, /compare,
  /how-it-works.

[2026-09-03] Expanded super_admin_account_specification.md (now v1.1)
  with Sections 3.7-3.14: Content Management/CMS, Buyer Management,
  Announcements, Product Management, Order Management, Admin Task
  Assignment, Customer Assignment to Admin, and the Full-Control
  Principle. Establishes that super-admin manages all visitor content,
  buyers, products, orders, and admin work assignment, on top of the
  existing admin-management/security-logs/account-activity/backups/
  vault pages. Spec-only change — no code built yet.

[2026-09-03] Added Section 9 (Recommended Improvements & Hardening,
  proposed/not yet built) to super_admin_account_specification.md
  (now v1.2): 2FA/MFA, IP allowlist + anomaly-blocking, break-glass
  recovery, product approval flow, customer-assignment exclusivity
  check, task notifications/escalation, CMS multi-version history,
  media library, scoped-by-default admin views, in-app notification
  center, full Analytics page, per-buyer data export, backup restore
  runbook. Testing checklist renumbered to Section 10, Change Log to
  Section 11. Spec-only change — no code built yet.

[2026-09-03] Added Section 12 (Implementation Plan, Phased) to
  super_admin_account_specification.md (now v1.3): 10-phase,
  dependency-ordered build sequence covering Sections 3, 7, and 9 in
  full — Phase 1 auth/2FA foundation through Phase 10 remaining
  hardening — each phase with scope, deliverables, and acceptance
  criteria. Spec-only change — no code built yet.
[2026-09-03] Added buyer_account_specification.md (new file) — Downloads/
  License Delivery, Profile, Payment Methods, Subscription Management,
  Support Tickets, Notifications. Companion to buyer_order_tracking_
  specification.md, covers everything else in the buyer dashboard
  recommendation review. Spec-only — no code built yet.

[2026-09-03] Added sitewide_technical_seo_specification.md (new file) —
  SEO infra (sitemap.ts/robots.ts), global error handling (root
  not-found.tsx/error.tsx), idle session timeout (Rule 32.5), and
  anonymized traffic analytics (Rule 41). No prior spec existed for
  any of these four items. Spec-only — no code built yet.

[2026-09-03] Added cart_checkout_specification.md (new file) — Cart
  (guest cookie + buyer-persisted), Checkout (digital vs physical
  shipping paths), PayMongo Payment Link + webhook integration per
  Rule 30, and Order/OrderItem creation. Verified as completely
  missing from the repo (no cart/checkout/paymongo files found
  anywhere) — this is the dependency every other buyer-facing spec
  (order tracking, buyer account) relies on. Spec-only — no code

[2026-09-03] Added additional_platform_gaps_specification.md (new
  file) — Coupon/Discount codes, Refund Request & Processing,
  Social/OAuth login (Google), Buyer 2FA (opt-in), Transactional
  email templates (Rule 35.5 wiring), Shop catalog filtering/sorting,
  and Developer environment setup (.env.example + testing baseline).
  Second-pass gap audit beyond cart_checkout_specification.md,
  buyer_account_specification.md, and
  sitewide_technical_seo_specification.md. Spec-only — no code built
  yet.

[2026-09-03] Added infra_ops_specification.md (new file) — CI/CD
  pipeline (.github/workflows/ci.yml), error tracking/monitoring
  (Sentry), security headers/CSP config for next.config.mjs, and
  Blog CMS admin write access. Third-pass gap audit beyond
  cart_checkout, buyer_account, sitewide_technical_seo, and
  additional_platform_gaps specs. Spec-only — no code built yet.

[2026-09-03] Added Section 5C (Spec Build Sequence) to
  overviewProject.txt — dependency-ordered master sequence across
  all 14 outstanding *_specification.md files in the repo (Cart/
  Checkout first as the foundation, through Infra/Ops last), cross-
  checked against every spec's own Status line. Also copied the
  standalone Bulk File Converter & PDF Invoice Renamer spec into the
  repo as bulk_file_converter_and_pdf_renamer_specification.md so it
  is tracked alongside the others. No feature code built — planning/
  documentation only.

[2026-09-04] Corrected stale tracking discovered during a full doc
  audit: overviewProject.txt Section 5B and
  buyer_homepage_specification_done.md Section 12 both still listed
  /products, the 6 category pages, product detail pages, /pricing,
  /compare, /faq, and the 4 legal pages as not-yet-built — all
  verified to already exist in the repo (cross-checked against
  improvement_1_done.md and live files). Updated both docs' status
  to reflect reality; only /case-studies (portfolio rebrand) and
  personalized recommendations are genuinely still open from that
  spec. Added PHASE 5B to Section 5C's build sequence for the
  homepage's Section 13 Front-End Modernization Enhancement Plan
  (the one substantial remaining item), and bumped that spec's
  Document Version to 1.2. No feature code touched — documentation
  correction only.

[2026-09-04] Phase 5B (Front-End Modernization, Hero + Quick Wins)
  audited against buyer_homepage_specification_done.md §13.2: both
  sections were already almost fully compliant (scroll-triggered
  whileInView entrances, crossfade+scale media transitions, mobile-
  reduced entrance distances, card hover lift via CSS, autoplay
  carousels already gated on prefers-reduced-motion via
  useMediaCarousel.ts) — Quick Wins needed no changes. HeroSection.tsx
  was the one gap: its scroll-linked parallax (a raw useTransform
  MotionValue piped into style.y) bypassed the root layout's
  <MotionConfig reducedMotion="user"> entirely, since that only gates
  animate/transition props, not live scroll values. Fixed by wiring in
  useIsMobileViewport + usePrefersReducedMotion (same hooks already
  used elsewhere on the homepage) and zeroing parallaxY when either
  applies. npx tsc --noEmit clean against this file (one pre-existing,
  unrelated error in services/prisma.ts — @prisma/client has not been
  generated in this environment, needs `npx prisma generate` against a
  live DATABASE_URL).

[2026-09-04] PHASE 5B closed out — audited every remaining homepage
  section (How It Works, Testimonials, FAQ Accordion, CTA Banner, all
  6 category sections, FeatureGrid, buyer dashboard) against
  buyer_homepage_specification_done.md §13.2. All already compliant:
  most category sections use the shared ScrollReveal primitive
  (components/shared/ScrollReveal.tsx) which already handles
  whileInView + mobile-reduced distance + reduced-motion; the two
  carousels (Featured Products, Testimonials) already gate their
  autoplay loop on prefers-reduced-motion and stack on mobile; buyer
  dashboard already has mount-in entrance and has no async data yet
  to skeleton. No further code changes needed beyond the earlier Hero
  parallax fix. Bumped buyer_homepage_specification_done.md to
  Document Version 1.3 — only personalized recommendations remains
  open against that document.

[2026-09-04] Cart/Checkout Phase 1, step 1/6 (cart_checkout_specification.md
  Section 9): added CartItem, Order, and OrderItem models to
  prisma/schema.prisma per the spec's Section 5 data model. Added two
  things beyond the spec's raw proposal per Rule 6 (sensitive-record
  handling): Order.updatedAt (optimistic-lock support) and
  Order.deletedAt (soft delete instead of hard delete), plus a
  @@unique constraint on Order.paymongoOrderId. No prior Order model
  existed in the schema, so this is a clean add, not a reconciliation.
  Schema not yet pushed to a live database — this sandbox has no
  DATABASE_URL/DIRECT_URL configured; `npx prisma db push` and
  `npx prisma generate` need to run in the actual dev environment
  against Supabase. Next: CartItem API (/api/cart) + cart drawer UI.

[2026-09-04] Cart/Checkout Phase 1, step 1b (cart_checkout_specification.md
  Section 9): built the cart drawer + CartItem API. New: lib/cartSession.ts
  (guest cart_token cookie vs buyer session identity, plus guest->buyer
  merge on first cart read after login), app/api/cart/route.ts (GET/POST/
  PUT/DELETE, ownership-checked, rate-limited via the existing
  lib/rateLimit.ts at 100/15min), context/CartContext.tsx (site-wide cart
  state + its own ToastStack, wraps every public page in
  app/(public)/layout.tsx), components/cart/CartDrawer.tsx + app/styles/
  cart.css (line items, quantity stepper, remove, subtotal, "Proceed to
  Checkout" link -- /checkout itself is step 1c, not built yet). Updated
  lib/productsData.ts (added getProductById()), components/shared/NavBar.tsx
  (cart icon + item-count badge, desktop + mobile), components/products/
  ProductDetail.tsx (Add to Cart button, variant-aware). No CSRF check on
  the cart endpoints -- matches this repo's existing convention (only auth
  endpoints enforce it per lib/csrf.ts's own header comment; app/api/
  contact/route.ts has none either), relying on SameSite=strict + rate
  limiting instead. Verified via `npx tsc --noEmit` and `npx eslint` against
  every new/changed file -- zero new errors, only the same pre-existing
  services/prisma.ts error (unGenerated Prisma client, needs a live
  DATABASE_URL) already noted in earlier entries; `npx prisma generate`
  itself could not be run in this sandbox either (binaries.prisma.sh is
  outside the allowed network egress list). Next: checkout page + server-
  side validation (step 1c).

[2026-09-04] Cart/Checkout Phase 1, step 1c (cart_checkout_specification.md
  Section 9): built the checkout page + server-side order-summary
  validation. New: app/api/checkout/validate/route.ts (GET; server-
  computed subtotal/shipping/total, drops any cart row whose product was
  retired and reports it back for a warning toast -- does NOT create an
  Order yet, that's step 1d, paired with the actual PayMongo Payment Link
  per spec Section 4.4's "before redirecting to PayMongo" wording),
  app/(public)/checkout/page.tsx + components/checkout/CheckoutForm.tsx +
  app/styles/checkout.css (contact info pre-filled for signed-in buyers,
  shipping address section only rendered when the cart has a physical
  item -- i.e. a t-shirts category product -- order summary sidebar,
  empty/loading/error states per Rule 25). Refactored the CartItem-to-
  catalog enrichment logic (previously only in app/api/cart/route.ts) out
  into lib/cartPricing.ts so both /api/cart and the new validate route
  share one subtotal/shipping calculation instead of drifting apart;
  app/api/cart/route.ts now re-exports CartItemView as an alias of
  cartPricing's CartLineItem so CartContext/CartDrawer's existing imports
  didn't need to change. lib/cartSession.ts's resolveCartIdentity() now
  also returns the signed-in buyer's email (same Supabase getUser() call,
  no extra request) for the checkout email prefill. The "Pay with
  PayMongo" button is intentionally disabled with an explanatory hint --
  wiring a fake submit here would leave a buyer thinking they paid when
  they didn't, worse than an honest "coming soon." Verified via
  `npx tsc --noEmit` and `npx eslint` against every new/changed file --
  zero new errors, only the same pre-existing services/prisma.ts error
  (unGenerated Prisma client). Also ran `npx next build`: it compiled
  cleanly past all routes/types and only failed at next/font's Google
  Fonts fetch, which this sandbox's network allowlist blocks
  (fonts.googleapis.com) -- expected to build fine in the real dev
  environment. Next: PayMongo Payment Link + real Order creation (1d).

[2026-09-04] Revised villa-azure-agreement-v8-DRAFT-with-trial.txt's Tier 1
  Free Trial Program: replaced the fee-free onboarding gap with a ₱5,000
  non-refundable Trial Onboarding Fee (credited against the ₱25,000
  Trial Conversion Fee on convert; forfeited on non-conversion). Added
  new Section 6A (Feature Exclusivity Options — Criteria 1 Shared,
  Criteria 2 Permanent Exclusivity flat ₱25k/month, Criteria 3
  Time-Limited 6-month exclusivity at ₱20k/month) to Section 6. Updated
  tier1_free_trial_subscription_specification.md to match (Open
  Question 1 resolved, Business Rules 3.1/3.3/3.4 updated, new 3.6
  added). One new [ASSUMPTION — CONFIRM] introduced re: Criteria 2/3
  stacking when both are active simultaneously. Contract-only change —
  not reviewed by a lawyer, no technical build touched.

[2026-09-04] Added Criteria 4 (Blanket Exclusivity Buyout, one-time
  ₱180,000) to Section 6A of villa-azure-agreement-v8-DRAFT-with-trial.txt
  -- one payment makes ALL future feature requests exclusive by default,
  Monthly Fee stays flat ₱15,000/month regardless of feature count.
  Clarified that the ₱15,000 Feature Fee across all four Criteria is a
  minimum only and scales per Section 6's existing complexity table
  (Simple/Moderate/Complex), not a flat rate. Buyout drafted as
  prospective-only (doesn't retroactively reduce Monthly Fee for
  features already under an active Criteria 2/3 election) -- flagged as
  a new [ASSUMPTION -- CONFIRM]. tier1_free_trial_subscription_
  specification.md's 3.6 updated to match. Contract-only change.
[2026-09-04] Rewrote villa-azure-agreement-v8-DRAFT-with-trial.txt
  Section 6A from a per-feature Criteria election model to an
  account-wide single-Criteria model: Client holds exactly one active
  Criteria for the whole account, not per feature. Electing a new
  Criteria auto-terminates the previous one (no more stacking
  ambiguity between Criteria 2 and 3). Added AUTO-REVERT ON
  SWITCH-DOWN rule: features that gained exclusivity under a
  terminated Criteria 2/3 automatically fold back into the shared
  template. Carved out Criteria 4 (Buyout) as the exception: down-
  switch forfeits the non-refundable P180,000 fee without reverting
  features, and up-switch into Criteria 4 retroactively makes all
  existing features (including prior Shared ones) exclusive --
  resolves the old prospective-only Buyout [ASSUMPTION -- CONFIRM].
  Left one new [ASSUMPTION -- CONFIRM] on Criteria 3's account-wide
  6-month clock behavior for features added mid-window. Contract-only
  change.

[2026-09-04] Removed the Tier 1 3-month Minimum Commitment / lock-in
  from villa-azure-agreement-v8-DRAFT-with-trial.txt entirely (Section
  2 base pricing + termination summary, Section 4A payment terms,
  Section 8A termination -- merged its two blocks into one lock-in-free
  cancellation policy and dropped the P15,000 early-termination fee,
  Section 17 termination table, and the Trial Conversion clause's now-
  moot lock-in-start-point bullet/assumption). Tier 1 clients can now
  cancel anytime with 1 month written notice, no minimum period. Also
  resolved part of the Free Trial eligibility [ASSUMPTION -- CONFIRM]:
  confirmed once-per-Client/business/resort; left open whether it's
  new-clients-only and whether Service Provider retains discretion to
  verify eligibility. Contract-only change.

[2026-09-04] Resolved the remaining Free Trial eligibility
  [ASSUMPTION -- CONFIRM] in villa-azure-agreement-v8-DRAFT-with-trial.txt
  (Section 2, Tier 1 Special Note): confirmed new-clients-only (Option
  A -- a Client with any prior Tier 1 history, Trial or paid, on this
  specific template is not eligible for a new Trial on that same
  template even if cancelled), and clarified the once-per-Client limit
  is scoped PER TEMPLATE PRODUCT (resort, inventory, photography, etc.)
  rather than a lifetime limit across all of the Service Provider's
  templates -- a Client who used their Trial on one template can still
  Trial a different template. Left one narrower [ASSUMPTION -- CONFIRM]
  open: whether the Service Provider retains discretion to verify
  eligibility before approving, or approval is automatic. Contract-only
  change.

[2026-09-04] Resolved a pre-existing internal contradiction in
  villa-azure-agreement-v8-DRAFT-with-trial.txt between Section 0
  ("Service Provider owns and maintains ALL infrastructure") and
  Sections 2/9 (which said "Client must create and maintain their own
  Vercel Pro account"). Per Client clarification: Service Provider
  creates a dedicated business email exclusively for each Tier 1
  Client, registers all infra accounts (Vercel, Supabase, R2, Google
  Drive, EmailJS) under that email, and retains sole control -- Client
  never creates/logs into these accounts but remains financially
  responsible for the pass-through costs, billed via Vic rather than
  paid directly to each provider. Rewrote Section 2's infrastructure
  cost block, the Trial Conversion clause's infra reference, and fully
  rewrote Section 9 (Vic's Role, Client Billing Flow) to match. Added
  an explicit "source code / GitHub repository access" exclusion to
  Tier 1's WHAT'S NOT INCLUDED list, confirming code never transfers
  to Tier 1 clients (unlike Tier 2/3/4). Left one new
  [ASSUMPTION -- CONFIRM] on the exact pass-through billing mechanism
  (combined single invoice vs. itemized). Contract-only change.

[2026-09-04] Made Tier 1's Monthly Service Fee fully all-inclusive in
  villa-azure-agreement-v8-DRAFT-with-trial.txt: Client confirmed Vic
  pays infrastructure costs (Vercel Pro, Supabase/R2/Google/EmailJS
  overages) directly out of the flat P15,000/month fee -- no more
  separate Client Infrastructure Costs line item, no pass-through
  billing/reimbursement. Removed the ~P16,500-17,500/month combined
  total throughout (Section 2 and Section 9) and replaced with a flat
  P15,000/month total; resolved the prior billing-mechanism
  [ASSUMPTION -- CONFIRM]. Also reinforced (per Client confirmation)
  that the dedicated business email is infra-accounts-only and never
  grants GitHub/code access, since the Client has not purchased the
  code itself. Contract-only change.

[2026-09-04] Resolved the final piece of item #1 (Free Trial
  eligibility) in villa-azure-agreement-v8-DRAFT-with-trial.txt: Client
  confirmed eligibility is purely objective (new client = one Free
  Trial available; old/returning client on that template = none), no
  subjective Service-Provider discretion/approval step beyond checking
  its own records. Item #1 is now fully resolved -- no remaining
  [ASSUMPTION -- CONFIRM] tags on Free Trial eligibility. Contract-only
  change.

[2026-09-05] Resolved the Criteria 3 (Time-Limited Exclusivity)
  stacking [ASSUMPTION -- CONFIRM] in
  villa-azure-agreement-v8-DRAFT-with-trial.txt: a new feature
  requested partway through an active Criteria 3 window now gets its
  own independent 6-month clock (from that feature's own
  request/completion date) instead of sharing one account-wide window
  -- so features under Criteria 3 can expire from exclusivity on
  different dates. Updated the expiry, per-feature auto-share, and
  per-feature renewal/upgrade bullets to match. Updated
  tier1_free_trial_subscription_specification.md Section 3.6's
  Criteria 3 bullet to match and marked it RESOLVED. Note: this is
  separate from the still-open item re: a client holding both an
  active Criteria 2 and Criteria 3 feature simultaneously (Section
  3.6's other Open item / contract's General Rules "only ONE active
  Criteria per account" language) -- that stacking question remains
  unresolved. Contract-only change -- not reviewed by a lawyer, no
  technical build touched.

[2026-09-05] Added trial_tracking_and_developer_notifications_specification.md
  (new file) -- extends the Tier 1 Free Trial Program with operational
  automation: trial day tracker, developer Telegram setup during the
  wizard, daily Telegram reminders to the developer (Day 20-30), a
  T-minus-8-hour auto-lockdown + PDF booking invoice (pending/booked/
  cancelled) auto-emailed to the resort owner on non-conversion, and
  late-conversion reversal (site/dashboard restored, invoice cancelled
  if not yet sent). Feature builds into the deployed client template
  itself (e.g. small-time-resort-template), not Matthew Studio, since
  booking data and the First-Run Setup Wizard already live there per
  deployment. Access control resolved as Telegram-confirmed conversion:
  clicking "Mark as Converted" in the super-admin dashboard only sends a
  one-time 6-digit code to the developer's Telegram -- the action can't
  finalize without that code, so the Client (who holds the super_admin
  role on their own site) can never self-convert. Spec-only -- no code
  built yet.

[2026-09-05] Resolved v2 review Item #1 (contract/spec timing mismatch)
  in villa-azure-agreement-v8-DRAFT-with-trial.txt: replaced the
  "[ASSUMPTION -- CONFIRM: within 24 hours of termination]" placeholder
  with the confirmed 8-hours-BEFORE-cutoff timing, matching Section 2.4
  (T-8h) of trial_tracking_and_developer_notifications_specification.md.
  Contract-only change -- no code built yet.

[2026-09-05] Resolved v2 review Items #2/#3 in
  trial_tracking_and_developer_notifications_specification.md by
  redesigning Section 5 around the hidden vault
  (/system-vault/[vaultSlug]) already built in
  small-time-resort-template (static branch) rather than a new
  Telegram-code system: "Mark as Converted" now lives inside the vault's
  Emergency Actions, gated by the vault's own passphrase+OTP (no
  super-admin identity behind it at all, per services/vaultAuth.js) --
  this resolves both who can finalize a conversion (developer only, by
  construction) and reaching it during full lockdown (vault was never
  inside the lockdown's blast radius). Section 2.2 changed from a
  wizard Telegram deep-link flow to two env vars set once during
  deployment and never exposed via any wizard/dashboard UI:
  VAULT_OWNER_EMAIL (already existed in the template) and the new
  VAULT_OWNER_TELEGRAM_CHAT_ID -- distinct from the dashboard-editable
  SystemSettings.adminTelegramChatIds used for client/staff booking
  alerts. Removed TrialStatus.telegramChatId and the
  conversionCodeHash/conversionCodeSentAt/conversionCodeExpires/
  conversionAttempts fields (Section 3/5) as no longer needed. Spec-only
  -- VAULT_OWNER_TELEGRAM_CHAT_ID and its wiring into
  services/vaultTelegramAlerts.js still need to be built in the actual
  template repo.

[2026-09-05] Resolved v2 review Item #4 (no fallback on Telegram send
  failure) in trial_tracking_and_developer_notifications_specification.md
  Section 2.3: Daily Developer Reminders (Day 20-30) now send via both
  Telegram AND email (VAULT_OWNER_EMAIL), matching the existing
  dual-channel pattern already used by the vault's OTP/passphrase sends.
  No retry-with-backoff added -- kept consistent with
  services/telegram.js's existing best-effort, never-throws design; the
  two-channel redundancy covers more failure modes than retrying one
  channel. Every attempt (success/failure, either channel) now logs to
  SecurityLog as trial_reminder_sent / trial_reminder_send_failed.
  Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #5 (invoice email destination) in
  trial_tracking_and_developer_notifications_specification.md Section
  2.4 step 3: recipient is now explicitly resolved via
  AdminProfile.isOwner: true -> Supabase adminClient.auth.admin.getUserById()
  for the real email, never any other super_admin/staff account's
  email. Missing isOwner profile or a failed lookup escalates to the
  developer over the same VAULT_OWNER_EMAIL/VAULT_OWNER_TELEGRAM_CHAT_ID
  dual channel from Item #4, instead of silently dropping the invoice.
  Every attempt logs to SecurityLog as trial_invoice_sent /
  trial_invoice_send_failed with the email masked. Spec-only -- no code
  built yet.
[2026-09-05] Resolved v2 review Item #6 (no job completion confirmation)
  in trial_tracking_and_developer_notifications_specification.md
  Section 2.4: added step 5 -- a single job-summary push notification to
  the developer (VAULT_OWNER_EMAIL + VAULT_OWNER_TELEGRAM_CHAT_ID dual
  channel) after the T-8h lockdown+invoice job finishes or crashes,
  listing per-step outcome (lockdown/invoice-generated/invoice-emailed)
  and overall status. Deliberately push, not just a dashboard log row,
  unlike the nightly backup job's silent BackupLog pattern (Rule 40.6) --
  this event is rare and high-stakes enough to alert on immediately.
  Logs to SecurityLog as trial_lockdown_job_completed /
  trial_lockdown_job_failed. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #7 (job idempotency on crash/retry)
  in trial_tracking_and_developer_notifications_specification.md
  Section 2.4: added a step-0 idempotency guard using the existing
  TrialStatus timestamp fields (lockedAt, invoiceGeneratedAt,
  invoiceEmailedAt -- no new fields needed) -- each of steps 1-3 checks
  its own marker first and skips if already set, writing the marker
  immediately on success rather than batching at the end. Makes the
  T-8h job safe to re-invoke after a mid-job crash without repeating a
  completed step -- specifically prevents the invoice email (step 3)
  from ever being sent twice. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #8 (missing unlockedAt field) in
  trial_tracking_and_developer_notifications_specification.md: added
  TrialStatus.unlockedAt (Section 3), set during Section 2.5's
  late-conversion reversal -- audit-trail counterpart to lockedAt, so a
  reversed lockdown is timestamped and distinguishable from a trial that
  never locked. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #9 (zero-bookings invoice) in
  trial_tracking_and_developer_notifications_specification.md Section
  2.4 step 2: invoice is still generated (never skipped) when a trial
  has zero bookings -- PDF states "No bookings were recorded during
  this trial period" instead of a blank table, no special-case branch
  in the job logic. Also closed out Section 6: all 9 v2-review items
  now resolved, noted explicitly; only the two unrelated open items
  (48-hour data-export confirm, Criteria 2/3 stacking question) remain.
  Spec-only -- no code built yet.

[2026-09-05] Corrected vault_specification.md (now v1.2) Section 9 and
  13 -- repo check against the shop branch found none of the claimed
  mockup files (app/superAdmin/vault, app/admin/vault, components/
  vault/*, lib/vaultMockData.ts) actually exist. Unchecked all of
  Section 9's Pages & UI items and rewrote Section 13 to state nothing
  is built, mockup or real. Spec-only -- no code built yet.

[2026-09-05] Added Section 2.3a (Day 27 Client Reminder) to
  trial_tracking_and_developer_notifications_specification.md (now
  v1.1) -- the contract-required one-time email reminder to the Client
  by Day 27, previously missing (only developer-facing reminders
  existed in Section 2.3). Added TrialStatus.day27ClientReminderSentAt
  and a Day 27 line to the User Flow. Spec-only -- no code built yet.

[2026-09-05] Corrected super_admin_account_specification.md (now v1.7)
  Section 3.6 -- it described a different, conflicting vault mechanism
  (on-demand DB-persisted secret key, Reason/Expiry form, revocable)
  from Section 7's session-slug + ephemeral-credentials design, which
  matches vault_specification.md and is the design of record. Replaced
  Section 3.6 with a pointer to Section 7. Also aligned the admin-
  permissions list, the /api/admin/vault/generate route (now /api/
  vault/credentials/generate), the testing checklist, and Phase 4's
  scope line to match. Spec-only -- no code built yet.
[2026-09-05] Added Product, ProductGalleryImage, AdminSession,
  VaultCredentials, GatekeeperEvent, and DeviceBan models to
  prisma/schema.prisma -- closes part of the Task 1 gap flagged
  earlier (missing Prisma models). AdminSession/VaultCredentials/
  GatekeeperEvent copied from vault_specification.md Section 4/12.7;
  DeviceBan copied from gatekeeper_specification.md Section 5.1;
  Product/ProductGalleryImage derived from lib/productsData.ts's
  actual interface plus product_media_upload_specification.md's media
  fields, since no formal Prisma block for base Product existed
  before. Deliberately did NOT add a Prisma User model --
  login_vault_page_secret_key_generation.md's User/password block
  conflicts with the real auth system (Supabase Auth only, role in
  user_metadata.role, per middleware.ts) and appears superseded by
  vault_specification.md. Also deleted the duplicate
  bulk-file-converter-and-pdf-renamer-spec.md (byte-identical to
  bulk_file_converter_and_pdf_renamer_specification.md). Schema
  written but not pushed to a live DB, and not run through
  `prisma validate` (no network access to Prisma's engine binaries in
  this environment) -- run `npx prisma validate` locally to confirm
  before `db push`.

[2026-09-05] Fixed Section 5C's build sequence in overviewProject.txt:
  (1) removed login_vault_page_secret_key_generation.md entirely --
  it was still numbered as item 6 (buildable), but its User/password
  design conflicts with the real Supabase-only auth system and is
  superseded by vault_specification.md, per super_admin_account_
  specification.md's own 2026-09-05 correction. (2) Added an explicit
  exclusion list documenting that tier1_free_trial_subscription_
  specification.md and trial_tracking_and_developer_notifications_
  specification.md were never missing by oversight -- both target a
  separate deployed-client-template codebase, not matthew-studio,
  per their own "where this gets built" notes; also listed villa-
  azure-agreement-v8-DRAFT-with-trial.txt as reference-only. Renum
  bered items 6-14 down to 6-13 to close the gap left by removing
  login_vault_page_secret_key_generation.md. (3) Added a note on item
  4 (admin_account_specification.md) that its Product CRUD must exist
  before item 9 (product_media_upload_specification.md) can add media
  fields to a real record -- product_media_upload's own spec only
  ever assumed a Product row already existed. Spec-only edit -- no
  application code changed.

[2026-09-05] Built cart_checkout_specification.md Phase 1 step 1d
  (PayMongo integration, Section 4.3/4.4): services/paymongo.ts
  (createCheckoutSession -- PayMongo Checkout Session product, not
  Payment Links, chosen for success_url/cancel_url + itemized
  line_items support the spec's product choice was left open on) and
  app/api/checkout/route.ts (POST -- rate-limited 10/15min per Rule
  32.1's payment tier, re-validates cart server-side via the existing
  loadCartLineItems/resolveCartIdentity helpers, creates Order+
  OrderItem as "pending", opens the PayMongo session, deletes the
  Order on PayMongo failure rather than leaving an orphaned pending
  row, clears the cart only after the session is created). Updated
  components/checkout/CheckoutForm.tsx: Pay with PayMongo button now
  submits to this route and does a full-page redirect to the returned
  checkoutUrl. Added PAYMONGO_SECRET_KEY to the env vars list (Section
  7), which was also corrected to list every var actually referenced
  in the codebase (was still just the original 3 Supabase keys).
  npx tsc --noEmit passes clean except the pre-existing "PrismaClient
  has no exported member" error, which is expected -- `prisma
  generate` has never been run in this sandbox (no network access to
  Prisma's engine binaries) or presumably against the new Product/
  AdminSession/etc. models from earlier today either; run `npx prisma
  generate` locally before testing this route. Order.status is never
  set to PAID by this route -- that's 1e (webhook), not built yet.
  order-confirmation/[orderId] route also doesn't exist yet (1f) --
  PayMongo's success_url points at it already, so it 404s today until
  1f is built.

[2026-09-05] Built cart_checkout_specification.md Phase 1 step 1e
  (webhook, Rule 30): app/api/paymongo/webhook/route.ts -- verifies
  PayMongo's Paymongo-Signature header (HMAC-SHA256 over the raw
  request body, constant-time compare against both test/live values)
  before trusting anything in the payload, matches the event to an
  Order by paymongoOrderId (the Checkout Session id from step 1d),
  and on a checkout_session.payment.paid event sets Order.status =
  "PAID" + paymongoPaymentId/paymentStatus/paidAt per Rule 30.1/30.2.
  Every outcome (missing/invalid signature, unhandled event type,
  successful payment) logs to SecurityLog via the existing
  logSecurityEvent helper. Added PAYMONGO_WEBHOOK_SECRET to the env
  vars list. Known gap, documented in the file header rather than
  silently worked around: a checkout_session.payment.failed event is
  acknowledged but doesn't change Order.status, since the Order model
  (Section 5) has no failed/cancelled-by-provider status distinct
  from "pending" -- needs a spec decision before a real fix. npx tsc
  --noEmit and npx eslint both pass clean (tsc's one error is the
  same pre-existing "PrismaClient has no exported member" -- prisma
  generate has never been run in this sandbox against the current
  schema; run it locally before testing this route).

[2026-09-05] Fixed cart-clear timing and added webhook self-heal —
  Gaps A & B from cart_checkout_specification.md Section 4.3:
  app/api/checkout/route.ts no longer clears the cart at Checkout
  Session creation (Gap A — a buyer whose connection died mid-payment
  was losing their cart even if payment never completed); it now
  only captures cartToken on the Order for guest carts. New
  lib/orderPayment.ts exports markOrderPaid() — a single, idempotent
  helper that flips Order.status to "PAID" (Rule 30.1/30.2 fields)
  and clears the cart only at that confirmed moment; both
  app/api/paymongo/webhook/route.ts and a new self-heal path now call
  it instead of each having their own copy. New
  app/api/orders/[orderId]/status/route.ts (Gap B) is the order-
  confirmation page's polling endpoint: once an Order has been
  "pending" longer than SELF_HEAL_THRESHOLD_SECONDS, it makes the one
  documented exception to Rule 30.3 and re-queries PayMongo directly
  for that Checkout Session, since by definition the webhook never
  arrived — runs markOrderPaid() if PayMongo confirms payment.
  services/paymongo.ts gained the session-lookup call this route
  needs. Added Order.cartToken to prisma/schema.prisma (Rule 21:
  run npx prisma db push + npx prisma generate locally).

[2026-09-05] Added the order confirmation page with status polling —
  completes cart_checkout_specification.md Section 9 step 6, Phase 1
  step 1f: app/(public)/order-confirmation/[orderId]/page.tsx (the
  route PayMongo's success_url already pointed at, per the 2026-09-05
  webhook entry above) and components/checkout/OrderConfirmation.tsx
  (Client Component, polls /api/orders/[orderId]/status on an
  interval until the Order leaves "pending", showing a pending/paid/
  failed state accordingly). New app/styles/checkout.css additions
  for the confirmation layout. components/checkout/CheckoutForm.tsx
  updated in step with the cart-clear timing fix above. Phase 1 of
  cart_checkout_specification.md (Cart → Checkout → PayMongo →
  Webhook → Order Confirmation) is now fully built end to end.

[2026-09-05] Built the FAILED payment status + retry flow (resolves
  the KNOWN GAP documented in app/api/paymongo/webhook/route.ts's
  header): added Order.status = "FAILED" (prisma/schema.prisma
  comment updated, no migration needed -- status is already a plain
  String field) and lib/orderPayment.ts's new markOrderFailed(),
  which -- unlike markOrderPaid() -- never clears the cart, so a
  failed order stays retryable. services/paymongo.ts gained a shared
  CheckoutLineItemInput type (createCheckoutSession no longer takes a
  cart-specific CartLineItem[]) plus isFailed/isExpired detection on
  both the webhook's extractPaymentDetails() and the self-heal
  getCheckoutSession() lookup. app/api/paymongo/webhook/route.ts now
  handles checkout_session.payment.failed by calling
  markOrderFailed(). app/api/orders/[orderId]/status/route.ts's
  self-heal path now also detects a failed or expired PayMongo
  session (not just a confirmed payment) and marks the Order FAILED
  instead of leaving it "pending" forever. New app/api/orders/
  [orderId]/retry-payment/route.ts opens a fresh PayMongo Checkout
  Session for the same Order (never a new Order -- Rule 30.3's one
  documented exception, same pattern as the self-heal route) using
  the Order's own OrderItem snapshots as line items; resolves the
  buyer's email via supabaseAdminClient.auth.admin.getUserById() for
  signed-in buyers since this app has no Prisma User model (Supabase
  Auth is the account source of truth -- this was the "thought there
  was a User Prisma model, there wasn't" bug from the prior session).
  KNOWN GAP carried into this route's header: Order doesn't persist
  the original shipping name/address/phone, so a retry rebuilds
  PayMongo billing with email only -- those fields are optional on
  PayMongo's side, so this still produces a valid Checkout Session.
  components/checkout/OrderConfirmation.tsx gained a "failed" state
  (Retry Payment button, calls the new route and redirects to its
  checkoutUrl) and a "timedOut" state (client-side 2-minute
  POLL_TIMEOUT_MS safety net with a manual "Check again" button,
  separate from and longer than the backend's own
  SELF_HEAL_THRESHOLD_SECONDS). app/styles/checkout.css gained
  .orderConfirmationFailed/.orderConfirmationTimedOut, same card
  shape as the existing confirmed state with error/warning accents.
  npm install run fresh in this sandbox to verify: npx tsc --noEmit
  and npx eslint both pass clean on every changed file -- the only
  tsc errors remaining are the same pre-existing "Module
  '@prisma/client' has no exported member" gap (binaries.prisma.sh is
  blocked in this sandbox, so `prisma generate` has never produced
  real types here; run it locally before testing this route).

[2026-09-06] Backfilled 5 unlogged commits found on 2026-09-06 during a
  Phase 2 status check (same kind of gap as the 2026-09-05 "log
  unlogged commits" entry above) — Section 5C in overviewProject.txt
  corrected to match:
  1. fc1d8a1 "add buyer downloads and license delivery page" —
     buyer_account_specification.md Section 4.1: app/buyer/downloads/
     page.tsx, components/buyer/DownloadsList.tsx,
     lib/hooks/useBuyerDownloads.ts, app/api/buyer/downloads/route.ts
     + [id]/file/route.ts (signed R2 download URLs via new
     services/r2.ts helper), lib/getSessionUserId.ts (Supabase Auth
     session helper — this app has no Prisma User model), new
     Download-related fields on prisma/schema.prisma.
  2. 47088c5 "add buyer profile page with avatar upload" —
     buyer_account_specification.md Section 4.2: app/buyer/profile/
     page.tsx, components/buyer/ProfileForm.tsx,
     lib/hooks/useBuyerProfile.ts, app/api/buyer/profile/route.ts +
     avatar/route.ts (uploads through lib/imageProcessor.ts's existing
     resize/WebP pipeline per Rule 35.6 before hitting R2).
  3. 31e5a55 "add device fingerprinting, geoip, and anomaly detection
     foundation" — super_admin_account_specification.md's own Section
     12 Phase 2 / Rule 38: lib/deviceFingerprint.ts (SHA-256 hash per
     Rule 38.4), services/geoip.ts (MaxMind GeoLite2 city-level
     lookup per Rule 38.5), lib/anomalyDetection.ts (impossible-travel
     + new-device detection per Rule 38.8), lib/securityLog.ts
     extended to populate the new fields, lib/accountActivity.ts
     (Rule 42's recordAccountActivity() helper), prisma/schema.prisma
     gained the SecurityLog/AccountActivityLog geo + device columns.
  4. e775106 "wire Gatekeeper device-ban enforcement into middleware
     and login" — gatekeeper_specification.md / Rule 47.3: new
     lib/gatekeeper.ts (checkDeviceBan() read-only lookup, fails open
     on DB error; evaluateGatekeeperTriggers() instant-ban on
     sql_injection_attempt/location_anomaly, 3-strike ban on
     login_failed/admin_login_denied/registration_abuse/
     rate_limit_hit/password_recovery_failed within a rolling 24h
     window counted directly from SecurityLog), wired into
     middleware.ts ahead of role-based routing and into
     app/api/auth/login/route.ts. NOT built yet: the
     /superAdmin/gatekeeper viewer/unban page — bans can currently
     only be lifted by hand in the DB.
  5. a8ca8f1 "build real super-admin dashboard with health widget and
     activity log" — super_admin_account_specification.md Section
     3.1: app/superAdmin/dashboard/page.tsx rebuilt on real data via
     new lib/dashboardStats.ts, app/superAdmin/layout.tsx and
     middleware.ts adjusted to match.
  None of these 5 commits are individually large enough to warrant
  reverting or re-sequencing — they're genuine progress — but Section
  5C's status markers were left stale (Phase 1 item 1f shown as not
  built when it was; Phase 2 item 3 shown as fully unstarted when 2 of
  its 6 pages exist; Phase 3 item 5 and Phase 4 item 7 shown as fully
  unstarted when both have real backend work landed, just missing
  their viewer-page deliverables). overviewProject.txt Section 5C
  updated in place to reflect actual status; no code was changed by
  this entry, documentation only.

[2026-09-06] Built buyer_order_tracking_specification.md Section 3.1
  (Order History list) — Phase 2 item 2, first half: new
  app/api/buyer/orders/route.ts (paginated, 10/page, scoped to the
  calling buyer's own userId per Section 2, joins each OrderItem's
  productId against the static lib/productsData.ts catalog via
  getProductById() -- not the empty Prisma Product table, since
  checkout stores productId from the static catalog, and the admin
  Product CRUD that would populate that table doesn't exist yet).
  New lib/orderStatus.ts centralizes the raw Order.status ->
  buyer-facing label/color mapping (Pending/Confirmed/Shipped/
  Delivered/Cancelled per spec, plus a "Payment Failed" bucket for
  the FAILED status the checkout spec added after this doc was
  written) and the 6-stage productionStage label map, shared with the
  detail page's timeline once that's built. New
  lib/hooks/useBuyerOrders.ts + components/buyer/OrdersList.tsx (all
  three Rule 25 states: skeleton, empty with CTA to /shop, error with
  retry) + app/buyer/orders/page.tsx + app/styles/buyerOrders.css,
  same card/skeleton pattern as buyerDownloads.css for visual
  consistency. Added a "Your orders" quick-link card to
  app/buyer/dashboard/page.tsx (new Package icon key in
  QuickLinkGrid.tsx's ICON_MAP) -- also removed that file's
  lucide-react import, which was already fully unused dead code
  before this change (QUICK_LINKS only ever used string icon keys,
  never the icon components directly). NOT built yet: Section 3.2
  Order Tracking Detail (/buyer/orders/[orderId]) -- timeline,
  cancel/reorder actions, GET .../[orderId] + PUT .../cancel
  endpoints. npx eslint clean except the same pre-existing
  set-state-in-effect finding already present in useBuyerDownloads.ts
  and useBuyerProfile.ts (this hook follows the same fetch-on-mount
  pattern by design, for consistency with those two). npx tsc --noEmit
  shows only the same pre-existing "@prisma/client has no exported
  member" cascade (binaries.prisma.sh blocked in this sandbox --
  prisma generate has never produced real types here; run it locally
  before testing this route).

[2026-09-06] buyer_order_tracking_specification.md Section 3.2 API
layer built: GET /api/buyer/orders/[orderId] (full detail, digital
3-step vs tshirt 6-stage timeline, ownership-checked 404) and PUT
/api/buyer/orders/[orderId]/cancel (status + productionStage guard,
409 on blocked cancel). Added productionStageHistory (Json),
courier, trackingNumber, shippingAddress fields to Order in
schema.prisma -- these were assumed to already exist by the spec but
were not found in the schema (admin's production-stage write
endpoint, admin_account_specification.md item 4, is still not built,
so these stay null/empty until that lands). NOT built yet: the
/buyer/orders/[orderId] page itself and the Cancel/Reorder/Contact
Support actions + toasts.

[2026-09-06] buyer_order_tracking_specification.md Section 3.2 page
built: app/buyer/orders/[orderId]/page.tsx + OrderTrackingDetail.tsx
(digital 3-step / tshirt 6-stage timeline, items, payment summary,
shipping, Cancel Order behind a new shared ConfirmationModal
component, Reorder via direct /api/cart calls + redirect to
/checkout, Contact Support link). Added useBuyerOrderDetail.ts hook
and app/styles/buyerOrderDetail.css. buyer_order_tracking_specification.md
is now fully built end to end (Sections 3.1 and 3.2). npm install
succeeded in this sandbox (previously untried); npx prisma generate
still fails on the same blocked binaries.prisma.sh check as before.
npx tsc --noEmit shows only the pre-existing implicit-any cascade on
Order.items already present in the sibling orders/route.ts (same
root cause -- Prisma client types aren't fully generated here) --
no new type errors introduced by this change.

[2026-09-06] buyer_account_specification.md Section 4.3 (Payment
Methods) task 1/4 built: added BuyerPaymentProfile and
SavedPaymentMethod models to schema.prisma. No local User/Buyer
table exists in this schema (auth is entirely Supabase) so
BuyerPaymentProfile holds the PayMongo Customer id per userId.
Design decision confirmed with developer: since PayMongo has no
standalone hosted "save card" flow (only vaults via a real Payment
Intent), Add Payment Method will use a minimal authorize-then-void
hold with setup_future_usage attached -- next tasks (2: paymongo.ts
helpers, 3: API endpoints, 4: UI page) build on this. prisma
validate/format both blocked by the same binaries.prisma.sh sandbox
restriction as before; schema block manually reviewed for syntax.

[2026-09-06] Split Phase 2 item 3's remaining 4.3 Payment Methods
  work into 3 micro-tasks under docs/tasks/ (Rule 49 Step 4):
  task-01-paymongo-vault-service.md (services/paymongo.ts vaulting
  helpers), task-02-api-payment-methods.md (list/add/remove/set-
  default routes), task-03-ui-payment-methods-page.md
  (/buyer/payment-methods page + component). No code changed. Build
  order is 01 -> 02 -> 03.

[2026-09-06] Task 01 complete: extended services/paymongo.ts with the
  vaulting helpers (getOrCreatePaymongoCustomer,
  createVaultingHold, getVaultedPaymentMethodFromHold,
  voidVaultingHold, detachPaymongoPaymentMethod) per
  docs/tasks/task-01-paymongo-vault-service.md. No API routes or UI
  in this task. (Logged retroactively — the code already existed on
  the shop branch when this session cloned it, but the change-log
  entry was missed at the time.)

[2026-09-06] Task 02 complete: added the 3 buyer payment-methods API
  routes per docs/tasks/task-02-api-payment-methods.md --
  app/api/buyer/payment-methods/route.ts (GET list, POST two-phase
  vaulting start/finalize), app/api/buyer/payment-methods/[id]/route.ts
  (DELETE, detaches on PayMongo then removes the row, promotes the
  next-newest card to default if the removed one was default),
  app/api/buyer/payment-methods/[id]/default/route.ts (PUT, single-
  transaction default reassignment). All three follow the existing
  buyer-route pattern (CSRF check, getSessionUser, ownership-scoped
  404s, Rule 28 response shape). npx prisma generate / npx tsc
  --noEmit still blocked by the same binaries.prisma.sh sandbox
  restriction as before -- not re-verified from this sandbox.

[2026-09-06] Task 03 complete: built the /buyer/payment-methods page
  per docs/tasks/task-03-ui-payment-methods-page.md --
  app/buyer/payment-methods/page.tsx, PaymentMethodsList.tsx
  (list/add/remove/set-default, Rule 25 loading/empty/error states,
  ConfirmationModal for remove), AddPaymentMethodForm.tsx (card
  number/expiry/cvc form), new lib/paymongoClient.ts (browser-side
  PayMongo publishable-key calls -- tokenizes the card and attaches
  it to the server-created hold; card digits never touch this app's
  own backend) and lib/hooks/useBuyerPaymentMethods.ts. Added 3D
  Secure handling: PaymongoClient's attach call surfaces a
  next_action redirect when required, the form sends the browser to
  PayMongo, and PaymentMethodsList picks the flow back up on return
  via a ?intent= query param and calls the API's finalize phase.
  Flipped the dashboard's "Add payment method" quick-link from
  available:false to the real /buyer/payment-methods href. New env
  var required: NEXT_PUBLIC_PAYMONGO_PUBLIC_KEY (publishable key,
  safe client-side per Rule 18.5/31.8 -- add to .env.local). This
  completes buyer_account_specification.md Section 4.3 end to end
  (Tasks 01-03). npx prisma generate / npx tsc --noEmit still
  blocked by the same binaries.prisma.sh sandbox restriction as
  before -- not re-verified from this sandbox; please run both
  locally before deploying.
[2026-09-06] Corrected overviewProject.txt Section 5C: Payment
  Methods (4.3) was still marked "API + UI not started" despite
  Tasks 01-03 already completing it end to end (see the three
  entries above). Flipped 4.3 to [DONE] with a Layer-3 verification
  note, updated the section's "remaining build order" line, and
  filed buyer_account_specification.md Section 4.4 (Subscription
  Management) as micro-tasks docs/tasks/task-04-schema-subscription.md,
  task-05-api-subscription.md, and task-06-ui-subscription-page.md.
  No feature code changed in this entry.

[2026-09-06] Task 04 complete: added Subscription and
  SubscriptionInvoice models to prisma/schema.prisma per
  docs/tasks/task-04-schema-subscription.md. npx prisma generate /
  db push not verified from this sandbox (binaries.prisma.sh
  blocked, same restriction as before) -- run both locally first.

[2026-09-06] Task 05 complete: added the 3 buyer subscription API
  routes per docs/tasks/task-05-api-subscription.md --
  app/api/buyer/subscription/route.ts (GET current plan/status,
  null-safe for buyers with no subscription), .../cancel/route.ts
  (POST, sets cancelAtPeriodEnd rather than deleting or cancelling
  immediately), .../invoices/route.ts (GET billing history, newest
  first). All three follow the existing buyer-route pattern
  (getSessionUser, Rule 28 response shape, CSRF check on the
  cancel mutation).

[2026-09-06] Task 06 complete: built the /buyer/subscription page
  per docs/tasks/task-06-ui-subscription-page.md --
  app/buyer/subscription/page.tsx, SubscriptionDetail.tsx (plan
  card, color-coded status badge, upgrade/downgrade link to
  /pricing, Cancel via the shared ConfirmationModal, billing
  history list with download links, Rule 25 loading/empty/error
  states), and lib/hooks/useBuyerSubscription.ts. Added a
  "Manage subscription" quick-link to the buyer dashboard
  (QuickLinkGrid gained a refresh-cw icon). This completes
  buyer_account_specification.md Section 4.4 end to end (Tasks
  04-06); corrected overviewProject.txt Section 5C's 4.4 line and
  "remaining build order" note to match. Upgrade/downgrade is a
  plain link to /pricing (no highlight query param wired yet) per
  the spec's own follow-up-phase allowance for full plan-change
  logic. npx prisma generate / npx tsc --noEmit still blocked by
  the binaries.prisma.sh sandbox restriction for the generate step;
  tsc --noEmit itself ran clean against these new files (only the
  same pre-existing, unrelated errors from the ungenerated Prisma
  client remain) -- please run prisma generate locally before
  deploying.

[2026-09-06] Task 07 complete: added SupportTicket and TicketMessage
  models to prisma/schema.prisma per
  docs/tasks/task-07-schema-support-tickets.md. npx prisma generate/
  format/validate still blocked by the binaries.prisma.sh sandbox
  restriction -- run npx prisma db push && npx prisma generate
  locally before continuing to Task 08.
[2026-09-06] Task 08 complete: added Support Tickets API routes per
  docs/tasks/task-08-api-support-tickets.md -- app/api/buyer/support/
  route.ts (list + create), app/api/buyer/support/[ticketId]/route.ts
  (detail + thread), app/api/buyer/support/[ticketId]/reply/route.ts
  (add message, auto-reopens a closed ticket), app/api/buyer/support/
  [ticketId]/reopen/route.ts (standalone reopen, idempotent no-op if
  already open). All four routes: CSRF-validated on mutations,
  session-scoped ownership check baked into the query (404 not 403 on
  mismatch), forbidden-character sanitization on subject/message
  fields (Rule 18.1), standard Rule 28 response shape. npx tsc
  --noEmit clean on these files except one implicit-any warning caused
  solely by the ungenerated Prisma client (SupportTicket/TicketMessage
  types don't exist until prisma generate runs) -- run npx prisma
  db push && npx prisma generate locally, which will also clear
  Task 07's pending client-regen note. Next: Task 09 (buyer/support
  list + new-ticket form UI) and Task 10 (ticket detail thread UI,
  plus fixing OrderTrackingDetail.tsx's "Contact Support" button to
  link into this system instead of the public /support contact form).[2026-09-06] Task 09 complete: built the /buyer/support list + new-
  ticket UI per docs/tasks/task-09-ui-support-tickets-list-page.md --
  app/buyer/support/page.tsx (Server Component, reads an optional
  ?orderId= search param ahead of Task 10's wiring),
  SupportTicketsList.tsx (card grid + pagination, mirrors
  OrdersList.tsx's loading/empty/error states per Rule 25),
  NewSupportTicketForm.tsx (subject/message fields with the same
  3-char/10-char minimums enforced client-side as
  app/api/buyer/support/route.ts enforces server-side, CSRF header,
  toast on success), lib/hooks/useBuyerSupportTickets.ts, and
  lib/ticketStatus.ts (open/answered/closed badge colors, mirrors
  lib/orderStatus.ts). Added a "Support" quick-link to the buyer
  dashboard (QuickLinkGrid gained a life-buoy icon). npx tsc --noEmit
  clean on all new/changed files -- only the same pre-existing,
  unrelated errors from the ungenerated Prisma client remain (16
  errors, none touching these files); npx prisma generate still
  blocked by the binaries.prisma.sh sandbox restriction noted since
  Task 04 -- run npx prisma db push && npx prisma generate locally
  before deploying. Next: Task 10 (ticket detail thread UI, plus
  fixing OrderTrackingDetail.tsx's "Contact Support" button to link
  into /buyer/support?orderId=... instead of the public /support
  contact form).
[2026-09-06] Task 10 complete: built the /buyer/support/[ticketId]
  thread UI per docs/tasks/task-10-ui-support-ticket-detail-page.md --
  app/buyer/support/[ticketId]/page.tsx (Server Component),
  SupportTicketThread.tsx (full message thread, buyer messages
  right-aligned vs admin left-aligned per senderRole, reply box that
  works on any status since the reply route itself auto-reopens a
  closed ticket, standalone "Reopen" button shown only when status is
  "closed", Rule 25 loading/not-found states), and
  lib/hooks/useBuyerSupportTicketDetail.ts (fetch + sendReply +
  reopenTicket, re-fetches after each mutation rather than optimistic
  local state so status flips always reflect the server). Also fixed
  OrderTrackingDetail.tsx's "Contact Support" button (flagged in the
  Task 07/08 entries above) -- it now links to
  /buyer/support?orderId=... instead of the public /support contact
  form, completing the pre-fill path SupportTicketsList.tsx (Task 09)
  already supported. This completes buyer_account_specification.md
  Section 4.5 end to end (Tasks 07-10); Section 5C's 4.5 line should
  be flipped to [DONE] on next audit. npx tsc --noEmit clean on all
  new/changed files -- only the same 16 pre-existing, unrelated
  errors from the ungenerated Prisma client remain; npx prisma
  generate still blocked by the binaries.prisma.sh sandbox
  restriction -- run npx prisma db push && npx prisma generate
  locally before deploying.
