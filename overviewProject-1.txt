CHANGE LOG (continued from overviewProject.txt, closed at 1000 lines)
--------------------------------------------------------
Full project overview (stack, file map, account types, spec build
sequence, etc.) stays authoritative in overviewProject.txt. This file
is change-log entries only, newest at the bottom, continuing where
that file left off.

[2026-09-03] Added additional_platform_gaps_specification.md (new
  file) — Coupon/Discount codes, Refund Request & Processing,
  Social/OAuth login (Google), Buyer 2FA (opt-in), Transactional
  email templates (Rule 35.5 wiring), Shop catalog filtering/sorting,
  and Developer environment setup (.env.example + testing baseline).
  Second-pass gap audit beyond cart_checkout_specification.md,
  buyer_account_specification.md, and
  sitewide_technical_seo_specification.md. Spec-only — no code built
  yet.

[2026-09-03] Added infra_ops_specification.md (new file) — CI/CD
  pipeline (.github/workflows/ci.yml), error tracking/monitoring
  (Sentry), security headers/CSP config for next.config.mjs, and
  Blog CMS admin write access. Third-pass gap audit beyond
  cart_checkout, buyer_account, sitewide_technical_seo, and
  additional_platform_gaps specs. Spec-only — no code built yet.

[2026-09-03] Added Section 5C (Spec Build Sequence) to
  overviewProject.txt — dependency-ordered master sequence across
  all 14 outstanding *_specification.md files in the repo (Cart/
  Checkout first as the foundation, through Infra/Ops last), cross-
  checked against every spec's own Status line. Also copied the
  standalone Bulk File Converter & PDF Invoice Renamer spec into the
  repo as bulk_file_converter_and_pdf_renamer_specification.md so it
  is tracked alongside the others. No feature code built — planning/
  documentation only.

[2026-09-04] Corrected stale tracking discovered during a full doc
  audit: overviewProject.txt Section 5B and
  buyer_homepage_specification_done.md Section 12 both still listed
  /products, the 6 category pages, product detail pages, /pricing,
  /compare, /faq, and the 4 legal pages as not-yet-built — all
  verified to already exist in the repo (cross-checked against
  improvement_1_done.md and live files). Updated both docs' status
  to reflect reality; only /case-studies (portfolio rebrand) and
  personalized recommendations are genuinely still open from that
  spec. Added PHASE 5B to Section 5C's build sequence for the
  homepage's Section 13 Front-End Modernization Enhancement Plan
  (the one substantial remaining item), and bumped that spec's
  Document Version to 1.2. No feature code touched — documentation
  correction only.

[2026-09-04] Phase 5B (Front-End Modernization, Hero + Quick Wins)
  audited against buyer_homepage_specification_done.md §13.2: both
  sections were already almost fully compliant (scroll-triggered
  whileInView entrances, crossfade+scale media transitions, mobile-
  reduced entrance distances, card hover lift via CSS, autoplay
  carousels already gated on prefers-reduced-motion via
  useMediaCarousel.ts) — Quick Wins needed no changes. HeroSection.tsx
  was the one gap: its scroll-linked parallax (a raw useTransform
  MotionValue piped into style.y) bypassed the root layout's
  <MotionConfig reducedMotion="user"> entirely, since that only gates
  animate/transition props, not live scroll values. Fixed by wiring in
  useIsMobileViewport + usePrefersReducedMotion (same hooks already
  used elsewhere on the homepage) and zeroing parallaxY when either
  applies. npx tsc --noEmit clean against this file (one pre-existing,
  unrelated error in services/prisma.ts — @prisma/client has not been
  generated in this environment, needs `npx prisma generate` against a
  live DATABASE_URL).

[2026-09-04] PHASE 5B closed out — audited every remaining homepage
  section (How It Works, Testimonials, FAQ Accordion, CTA Banner, all
  6 category sections, FeatureGrid, buyer dashboard) against
  buyer_homepage_specification_done.md §13.2. All already compliant:
  most category sections use the shared ScrollReveal primitive
  (components/shared/ScrollReveal.tsx) which already handles
  whileInView + mobile-reduced distance + reduced-motion; the two
  carousels (Featured Products, Testimonials) already gate their
  autoplay loop on prefers-reduced-motion and stack on mobile; buyer
  dashboard already has mount-in entrance and has no async data yet
  to skeleton. No further code changes needed beyond the earlier Hero
  parallax fix. Bumped buyer_homepage_specification_done.md to
  Document Version 1.3 — only personalized recommendations remains
  open against that document.

[2026-09-04] Cart/Checkout Phase 1, step 1/6 (cart_checkout_specification.md
  Section 9): added CartItem, Order, and OrderItem models to
  prisma/schema.prisma per the spec's Section 5 data model. Added two
  things beyond the spec's raw proposal per Rule 6 (sensitive-record
  handling): Order.updatedAt (optimistic-lock support) and
  Order.deletedAt (soft delete instead of hard delete), plus a
  @@unique constraint on Order.paymongoOrderId. No prior Order model
  existed in the schema, so this is a clean add, not a reconciliation.
  Schema not yet pushed to a live database — this sandbox has no
  DATABASE_URL/DIRECT_URL configured; `npx prisma db push` and
  `npx prisma generate` need to run in the actual dev environment
  against Supabase. Next: CartItem API (/api/cart) + cart drawer UI.

[2026-09-04] Cart/Checkout Phase 1, step 1b (cart_checkout_specification.md
  Section 9): built the cart drawer + CartItem API. New: lib/cartSession.ts
  (guest cart_token cookie vs buyer session identity, plus guest->buyer
  merge on first cart read after login), app/api/cart/route.ts (GET/POST/
  PUT/DELETE, ownership-checked, rate-limited via the existing
  lib/rateLimit.ts at 100/15min), context/CartContext.tsx (site-wide cart
  state + its own ToastStack, wraps every public page in
  app/(public)/layout.tsx), components/cart/CartDrawer.tsx + app/styles/
  cart.css (line items, quantity stepper, remove, subtotal, "Proceed to
  Checkout" link -- /checkout itself is step 1c, not built yet). Updated
  lib/productsData.ts (added getProductById()), components/shared/NavBar.tsx
  (cart icon + item-count badge, desktop + mobile), components/products/
  ProductDetail.tsx (Add to Cart button, variant-aware). No CSRF check on
  the cart endpoints -- matches this repo's existing convention (only auth
  endpoints enforce it per lib/csrf.ts's own header comment; app/api/
  contact/route.ts has none either), relying on SameSite=strict + rate
  limiting instead. Verified via `npx tsc --noEmit` and `npx eslint` against
  every new/changed file -- zero new errors, only the same pre-existing
  services/prisma.ts error (unGenerated Prisma client, needs a live
  DATABASE_URL) already noted in earlier entries; `npx prisma generate`
  itself could not be run in this sandbox either (binaries.prisma.sh is
  outside the allowed network egress list). Next: checkout page + server-
  side validation (step 1c).

[2026-09-04] Cart/Checkout Phase 1, step 1c (cart_checkout_specification.md
  Section 9): built the checkout page + server-side order-summary
  validation. New: app/api/checkout/validate/route.ts (GET; server-
  computed subtotal/shipping/total, drops any cart row whose product was
  retired and reports it back for a warning toast -- does NOT create an
  Order yet, that's step 1d, paired with the actual PayMongo Payment Link
  per spec Section 4.4's "before redirecting to PayMongo" wording),
  app/(public)/checkout/page.tsx + components/checkout/CheckoutForm.tsx +
  app/styles/checkout.css (contact info pre-filled for signed-in buyers,
  shipping address section only rendered when the cart has a physical
  item -- i.e. a t-shirts category product -- order summary sidebar,
  empty/loading/error states per Rule 25). Refactored the CartItem-to-
  catalog enrichment logic (previously only in app/api/cart/route.ts) out
  into lib/cartPricing.ts so both /api/cart and the new validate route
  share one subtotal/shipping calculation instead of drifting apart;
  app/api/cart/route.ts now re-exports CartItemView as an alias of
  cartPricing's CartLineItem so CartContext/CartDrawer's existing imports
  didn't need to change. lib/cartSession.ts's resolveCartIdentity() now
  also returns the signed-in buyer's email (same Supabase getUser() call,
  no extra request) for the checkout email prefill. The "Pay with
  PayMongo" button is intentionally disabled with an explanatory hint --
  wiring a fake submit here would leave a buyer thinking they paid when
  they didn't, worse than an honest "coming soon." Verified via
  `npx tsc --noEmit` and `npx eslint` against every new/changed file --
  zero new errors, only the same pre-existing services/prisma.ts error
  (unGenerated Prisma client). Also ran `npx next build`: it compiled
  cleanly past all routes/types and only failed at next/font's Google
  Fonts fetch, which this sandbox's network allowlist blocks
  (fonts.googleapis.com) -- expected to build fine in the real dev
  environment. Next: PayMongo Payment Link + real Order creation (1d).

[2026-09-04] Revised villa-azure-agreement-v8-DRAFT-with-trial.txt's Tier 1
  Free Trial Program: replaced the fee-free onboarding gap with a ₱5,000
  non-refundable Trial Onboarding Fee (credited against the ₱25,000
  Trial Conversion Fee on convert; forfeited on non-conversion). Added
  new Section 6A (Feature Exclusivity Options — Criteria 1 Shared,
  Criteria 2 Permanent Exclusivity flat ₱25k/month, Criteria 3
  Time-Limited 6-month exclusivity at ₱20k/month) to Section 6. Updated
  tier1_free_trial_subscription_specification.md to match (Open
  Question 1 resolved, Business Rules 3.1/3.3/3.4 updated, new 3.6
  added). One new [ASSUMPTION — CONFIRM] introduced re: Criteria 2/3
  stacking when both are active simultaneously. Contract-only change —
  not reviewed by a lawyer, no technical build touched.

[2026-09-04] Added Criteria 4 (Blanket Exclusivity Buyout, one-time
  ₱180,000) to Section 6A of villa-azure-agreement-v8-DRAFT-with-trial.txt
  -- one payment makes ALL future feature requests exclusive by default,
  Monthly Fee stays flat ₱15,000/month regardless of feature count.
  Clarified that the ₱15,000 Feature Fee across all four Criteria is a
  minimum only and scales per Section 6's existing complexity table
  (Simple/Moderate/Complex), not a flat rate. Buyout drafted as
  prospective-only (doesn't retroactively reduce Monthly Fee for
  features already under an active Criteria 2/3 election) -- flagged as
  a new [ASSUMPTION -- CONFIRM]. tier1_free_trial_subscription_
  specification.md's 3.6 updated to match. Contract-only change.
[2026-09-04] Rewrote villa-azure-agreement-v8-DRAFT-with-trial.txt
  Section 6A from a per-feature Criteria election model to an
  account-wide single-Criteria model: Client holds exactly one active
  Criteria for the whole account, not per feature. Electing a new
  Criteria auto-terminates the previous one (no more stacking
  ambiguity between Criteria 2 and 3). Added AUTO-REVERT ON
  SWITCH-DOWN rule: features that gained exclusivity under a
  terminated Criteria 2/3 automatically fold back into the shared
  template. Carved out Criteria 4 (Buyout) as the exception: down-
  switch forfeits the non-refundable P180,000 fee without reverting
  features, and up-switch into Criteria 4 retroactively makes all
  existing features (including prior Shared ones) exclusive --
  resolves the old prospective-only Buyout [ASSUMPTION -- CONFIRM].
  Left one new [ASSUMPTION -- CONFIRM] on Criteria 3's account-wide
  6-month clock behavior for features added mid-window. Contract-only
  change.

[2026-09-04] Removed the Tier 1 3-month Minimum Commitment / lock-in
  from villa-azure-agreement-v8-DRAFT-with-trial.txt entirely (Section
  2 base pricing + termination summary, Section 4A payment terms,
  Section 8A termination -- merged its two blocks into one lock-in-free
  cancellation policy and dropped the P15,000 early-termination fee,
  Section 17 termination table, and the Trial Conversion clause's now-
  moot lock-in-start-point bullet/assumption). Tier 1 clients can now
  cancel anytime with 1 month written notice, no minimum period. Also
  resolved part of the Free Trial eligibility [ASSUMPTION -- CONFIRM]:
  confirmed once-per-Client/business/resort; left open whether it's
  new-clients-only and whether Service Provider retains discretion to
  verify eligibility. Contract-only change.

[2026-09-04] Resolved the remaining Free Trial eligibility
  [ASSUMPTION -- CONFIRM] in villa-azure-agreement-v8-DRAFT-with-trial.txt
  (Section 2, Tier 1 Special Note): confirmed new-clients-only (Option
  A -- a Client with any prior Tier 1 history, Trial or paid, on this
  specific template is not eligible for a new Trial on that same
  template even if cancelled), and clarified the once-per-Client limit
  is scoped PER TEMPLATE PRODUCT (resort, inventory, photography, etc.)
  rather than a lifetime limit across all of the Service Provider's
  templates -- a Client who used their Trial on one template can still
  Trial a different template. Left one narrower [ASSUMPTION -- CONFIRM]
  open: whether the Service Provider retains discretion to verify
  eligibility before approving, or approval is automatic. Contract-only
  change.

[2026-09-04] Resolved a pre-existing internal contradiction in
  villa-azure-agreement-v8-DRAFT-with-trial.txt between Section 0
  ("Service Provider owns and maintains ALL infrastructure") and
  Sections 2/9 (which said "Client must create and maintain their own
  Vercel Pro account"). Per Client clarification: Service Provider
  creates a dedicated business email exclusively for each Tier 1
  Client, registers all infra accounts (Vercel, Supabase, R2, Google
  Drive, EmailJS) under that email, and retains sole control -- Client
  never creates/logs into these accounts but remains financially
  responsible for the pass-through costs, billed via Vic rather than
  paid directly to each provider. Rewrote Section 2's infrastructure
  cost block, the Trial Conversion clause's infra reference, and fully
  rewrote Section 9 (Vic's Role, Client Billing Flow) to match. Added
  an explicit "source code / GitHub repository access" exclusion to
  Tier 1's WHAT'S NOT INCLUDED list, confirming code never transfers
  to Tier 1 clients (unlike Tier 2/3/4). Left one new
  [ASSUMPTION -- CONFIRM] on the exact pass-through billing mechanism
  (combined single invoice vs. itemized). Contract-only change.

[2026-09-04] Made Tier 1's Monthly Service Fee fully all-inclusive in
  villa-azure-agreement-v8-DRAFT-with-trial.txt: Client confirmed Vic
  pays infrastructure costs (Vercel Pro, Supabase/R2/Google/EmailJS
  overages) directly out of the flat P15,000/month fee -- no more
  separate Client Infrastructure Costs line item, no pass-through
  billing/reimbursement. Removed the ~P16,500-17,500/month combined
  total throughout (Section 2 and Section 9) and replaced with a flat
  P15,000/month total; resolved the prior billing-mechanism
  [ASSUMPTION -- CONFIRM]. Also reinforced (per Client confirmation)
  that the dedicated business email is infra-accounts-only and never
  grants GitHub/code access, since the Client has not purchased the
  code itself. Contract-only change.

[2026-09-04] Resolved the final piece of item #1 (Free Trial
  eligibility) in villa-azure-agreement-v8-DRAFT-with-trial.txt: Client
  confirmed eligibility is purely objective (new client = one Free
  Trial available; old/returning client on that template = none), no
  subjective Service-Provider discretion/approval step beyond checking
  its own records. Item #1 is now fully resolved -- no remaining
  [ASSUMPTION -- CONFIRM] tags on Free Trial eligibility. Contract-only
  change.

[2026-09-05] Resolved the Criteria 3 (Time-Limited Exclusivity)
  stacking [ASSUMPTION -- CONFIRM] in
  villa-azure-agreement-v8-DRAFT-with-trial.txt: a new feature
  requested partway through an active Criteria 3 window now gets its
  own independent 6-month clock (from that feature's own
  request/completion date) instead of sharing one account-wide window
  -- so features under Criteria 3 can expire from exclusivity on
  different dates. Updated the expiry, per-feature auto-share, and
  per-feature renewal/upgrade bullets to match. Updated
  tier1_free_trial_subscription_specification.md Section 3.6's
  Criteria 3 bullet to match and marked it RESOLVED. Note: this is
  separate from the still-open item re: a client holding both an
  active Criteria 2 and Criteria 3 feature simultaneously (Section
  3.6's other Open item / contract's General Rules "only ONE active
  Criteria per account" language) -- that stacking question remains
  unresolved. Contract-only change -- not reviewed by a lawyer, no
  technical build touched.

[2026-09-05] Added trial_tracking_and_developer_notifications_specification.md
  (new file) -- extends the Tier 1 Free Trial Program with operational
  automation: trial day tracker, developer Telegram setup during the
  wizard, daily Telegram reminders to the developer (Day 20-30), a
  T-minus-8-hour auto-lockdown + PDF booking invoice (pending/booked/
  cancelled) auto-emailed to the resort owner on non-conversion, and
  late-conversion reversal (site/dashboard restored, invoice cancelled
  if not yet sent). Feature builds into the deployed client template
  itself (e.g. small-time-resort-template), not Matthew Studio, since
  booking data and the First-Run Setup Wizard already live there per
  deployment. Access control resolved as Telegram-confirmed conversion:
  clicking "Mark as Converted" in the super-admin dashboard only sends a
  one-time 6-digit code to the developer's Telegram -- the action can't
  finalize without that code, so the Client (who holds the super_admin
  role on their own site) can never self-convert. Spec-only -- no code
  built yet.

[2026-09-05] Resolved v2 review Item #1 (contract/spec timing mismatch)
  in villa-azure-agreement-v8-DRAFT-with-trial.txt: replaced the
  "[ASSUMPTION -- CONFIRM: within 24 hours of termination]" placeholder
  with the confirmed 8-hours-BEFORE-cutoff timing, matching Section 2.4
  (T-8h) of trial_tracking_and_developer_notifications_specification.md.
  Contract-only change -- no code built yet.

[2026-09-05] Resolved v2 review Items #2/#3 in
  trial_tracking_and_developer_notifications_specification.md by
  redesigning Section 5 around the hidden vault
  (/system-vault/[vaultSlug]) already built in
  small-time-resort-template (static branch) rather than a new
  Telegram-code system: "Mark as Converted" now lives inside the vault's
  Emergency Actions, gated by the vault's own passphrase+OTP (no
  super-admin identity behind it at all, per services/vaultAuth.js) --
  this resolves both who can finalize a conversion (developer only, by
  construction) and reaching it during full lockdown (vault was never
  inside the lockdown's blast radius). Section 2.2 changed from a
  wizard Telegram deep-link flow to two env vars set once during
  deployment and never exposed via any wizard/dashboard UI:
  VAULT_OWNER_EMAIL (already existed in the template) and the new
  VAULT_OWNER_TELEGRAM_CHAT_ID -- distinct from the dashboard-editable
  SystemSettings.adminTelegramChatIds used for client/staff booking
  alerts. Removed TrialStatus.telegramChatId and the
  conversionCodeHash/conversionCodeSentAt/conversionCodeExpires/
  conversionAttempts fields (Section 3/5) as no longer needed. Spec-only
  -- VAULT_OWNER_TELEGRAM_CHAT_ID and its wiring into
  services/vaultTelegramAlerts.js still need to be built in the actual
  template repo.

[2026-09-05] Resolved v2 review Item #4 (no fallback on Telegram send
  failure) in trial_tracking_and_developer_notifications_specification.md
  Section 2.3: Daily Developer Reminders (Day 20-30) now send via both
  Telegram AND email (VAULT_OWNER_EMAIL), matching the existing
  dual-channel pattern already used by the vault's OTP/passphrase sends.
  No retry-with-backoff added -- kept consistent with
  services/telegram.js's existing best-effort, never-throws design; the
  two-channel redundancy covers more failure modes than retrying one
  channel. Every attempt (success/failure, either channel) now logs to
  SecurityLog as trial_reminder_sent / trial_reminder_send_failed.
  Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #5 (invoice email destination) in
  trial_tracking_and_developer_notifications_specification.md Section
  2.4 step 3: recipient is now explicitly resolved via
  AdminProfile.isOwner: true -> Supabase adminClient.auth.admin.getUserById()
  for the real email, never any other super_admin/staff account's
  email. Missing isOwner profile or a failed lookup escalates to the
  developer over the same VAULT_OWNER_EMAIL/VAULT_OWNER_TELEGRAM_CHAT_ID
  dual channel from Item #4, instead of silently dropping the invoice.
  Every attempt logs to SecurityLog as trial_invoice_sent /
  trial_invoice_send_failed with the email masked. Spec-only -- no code
  built yet.
[2026-09-05] Resolved v2 review Item #6 (no job completion confirmation)
  in trial_tracking_and_developer_notifications_specification.md
  Section 2.4: added step 5 -- a single job-summary push notification to
  the developer (VAULT_OWNER_EMAIL + VAULT_OWNER_TELEGRAM_CHAT_ID dual
  channel) after the T-8h lockdown+invoice job finishes or crashes,
  listing per-step outcome (lockdown/invoice-generated/invoice-emailed)
  and overall status. Deliberately push, not just a dashboard log row,
  unlike the nightly backup job's silent BackupLog pattern (Rule 40.6) --
  this event is rare and high-stakes enough to alert on immediately.
  Logs to SecurityLog as trial_lockdown_job_completed /
  trial_lockdown_job_failed. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #7 (job idempotency on crash/retry)
  in trial_tracking_and_developer_notifications_specification.md
  Section 2.4: added a step-0 idempotency guard using the existing
  TrialStatus timestamp fields (lockedAt, invoiceGeneratedAt,
  invoiceEmailedAt -- no new fields needed) -- each of steps 1-3 checks
  its own marker first and skips if already set, writing the marker
  immediately on success rather than batching at the end. Makes the
  T-8h job safe to re-invoke after a mid-job crash without repeating a
  completed step -- specifically prevents the invoice email (step 3)
  from ever being sent twice. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #8 (missing unlockedAt field) in
  trial_tracking_and_developer_notifications_specification.md: added
  TrialStatus.unlockedAt (Section 3), set during Section 2.5's
  late-conversion reversal -- audit-trail counterpart to lockedAt, so a
  reversed lockdown is timestamped and distinguishable from a trial that
  never locked. Spec-only -- no code built yet.

[2026-09-05] Resolved v2 review Item #9 (zero-bookings invoice) in
  trial_tracking_and_developer_notifications_specification.md Section
  2.4 step 2: invoice is still generated (never skipped) when a trial
  has zero bookings -- PDF states "No bookings were recorded during
  this trial period" instead of a blank table, no special-case branch
  in the job logic. Also closed out Section 6: all 9 v2-review items
  now resolved, noted explicitly; only the two unrelated open items
  (48-hour data-export confirm, Criteria 2/3 stacking question) remain.
  Spec-only -- no code built yet.