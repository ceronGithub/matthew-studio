========================================================
MATTHEW STUDIO — PROJECT OVERVIEW & BUILD PLAN
========================================================

1. PROJECT NAME & PURPOSE
--------------------------------------------------------
Matthew Studio — a portfolio + shop site showcasing a resort
booking website template. Visitors browse past client work,
compare the 3 pricing tiers (managed SaaS / self-hosted /
custom build), learn what the template includes, read
testimonials, and book a demo. A single superAdmin account
manages the backend content (portfolio entries, pricing,
testimonials).

2. TECH STACK
--------------------------------------------------------
- Next.js 16.3.0 (App Router, Turbopack)
- React 19.2.8
- TypeScript
- Tailwind CSS v4 (+ custom design token system in
  app/styles/globals.css)
- framer-motion — section entrance animations
- lucide-react — icons
- axios — client-side HTTP calls
- Supabase — auth (superAdmin login) + database
- Fonts: Geist Sans / Geist Mono (next/font)

3. ACCOUNT TYPES
--------------------------------------------------------
- Public (no login) — all marketing/shop pages
- superAdmin — protected dashboard at /superAdmin,
  Supabase-authenticated, role checked via user_metadata.role

4. FILE MAP (as of this build)
--------------------------------------------------------
app/layout.tsx                  Root shell — fonts, global CSS only
app/styles/globals.css          Design tokens (color, spacing, z-index, transitions)
app/styles/shared.css           NavBar + Footer styles
app/styles/mediaQueries.css     All responsive breakpoints
app/(public)/layout.tsx         Public route group — renders NavBar + Footer
app/(public)/page.tsx           Home/Landing page
app/styles/home.css             Home page styles
app/api/auth/login/route.ts     superAdmin login endpoint (Supabase)
app/(auth)/                     Login page route (not yet built)
app/superAdmin/                 Admin dashboard (not yet built)
app/(public)/portfolio/page.tsx           Portfolio/Showcase grid (/portfolio)
app/(public)/portfolio/[slug]/page.tsx    Case study detail (/portfolio/[slug])
app/(public)/portfolio/[slug]/not-found.tsx  Invalid-slug 404 state
app/styles/portfolio.css        Portfolio grid + case study styles
components/portfolio/PortfolioGrid.tsx    Animated case study card grid
components/portfolio/CaseStudyDetail.tsx  Case study problem/solution/results layout
lib/portfolioData.ts            Static placeholder case study data (4 projects)
app/(public)/shop/page.tsx      Template Shop pricing tier grid (/shop)
app/styles/shop.css             Shop page + pricing card styles
components/shop/PricingGrid.tsx Animated pricing tier card grid
lib/pricingData.ts              Static placeholder pricing tier data (3 tiers)
app/(public)/features/page.tsx  Why Choose This Template page (/features)
app/styles/features.css         Tech stack, included-features, ROI, comparison table styles
components/features/ROICalculator.tsx  Client-side hours/money-saved calculator
lib/featuresData.ts             Static tech stack, included features, comparison rows
app/(public)/how-it-works/page.tsx  How It Works page (/how-it-works)
app/styles/how-it-works.css     Tab list + step timeline styles
components/how-it-works/OnboardingFlow.tsx  Client tier-tab switcher + step timeline
lib/howItWorksData.ts           Static per-tier onboarding step data
app/(public)/testimonials/page.tsx  Testimonials page (/testimonials)
app/styles/testimonials.css     Quote card grid styles
components/testimonials/TestimonialGrid.tsx  Animated testimonial quote card grid
lib/testimonialsData.ts         Static testimonial quotes (4 clients, matches portfolio)
app/(public)/blog/page.tsx      Blog index — programming tutorials (/blog)
app/(public)/blog/[slug]/page.tsx           Tutorial detail (/blog/[slug])
app/(public)/blog/[slug]/not-found.tsx      Invalid-slug 404 state
app/styles/blog.css             Blog grid + tutorial detail styles
components/blog/BlogGrid.tsx    Animated tutorial card grid
components/blog/TutorialDetail.tsx  Renders a tutorial's heading/paragraph/code blocks
lib/blogData.ts                 Placeholder tutorial content — paste new posts here
lib/socialLinksData.ts          Social media link config (Footer + Blog header)
components/shared/SocialLinks.tsx  Social icon row, reused in Footer + Blog page
components/shared/NavBar.tsx    Site nav, mobile menu
components/shared/Footer.tsx    Site footer (Blog link, social icons)
app/(public)/contact/page.tsx   Contact/Get Demo page (/contact)
app/api/contact/route.ts        Contact form submission handler (validates, logs — no email/DB delivery wired yet)
app/styles/contact.css          Contact page + form styles
components/contact/ContactForm.tsx  Client form: name/email/resort/tier/message, ?tier= prefill
lib/contactData.ts              Tier select options + calendar booking link
components/home/HeroSection.tsx Home hero + CTAs
components/home/QuickWins.tsx   Client wordmarks + result stats
lib/categoryShowcaseData.ts     6 marketplace category cards (static, pending products table)
components/home/CategoryShowcase.tsx  Homepage 6-card category grid, each links to /shop?category=
lib/featuredProductsData.ts     "Bestsellers This Month" static product list
components/home/FeaturedProducts.tsx  Homepage auto-scroll product carousel (pause on hover, arrows, mobile stack)
lib/howItWorksHomeData.ts       Universal 3-step (Browse/Choose/Start) homepage data
components/home/HowItWorksSection.tsx  Homepage 3-step How It Works section (separate from the
                                 per-tier OnboardingFlow on the standalone /how-it-works page)
lib/homeTestimonialsData.ts     "What Our Creators Say" homepage carousel data (6 quotes, one
                                 per marketplace category) — distinct from lib/testimonialsData.ts
                                 (resort-owner quotes on the standalone /testimonials page)
components/home/TestimonialsSection.tsx  Homepage auto-scroll testimonials carousel (right-to-left,
                                 18s loop, pause on hover, arrows, mobile stack) — same pattern as
                                 FeaturedProducts.tsx, opposite scroll direction
lib/homeFaqData.ts              Homepage FAQ Accordion data (6 general marketplace questions)
components/home/FAQAccordion.tsx  Homepage FAQ accordion — single-open expand/collapse, chevron
                                 rotate, height/opacity animation, "See All FAQs" CTA to /faq
components/home/CTABanner.tsx   Homepage bottom "Ready to Get Started?" CTA banner — Browse All
                                 Products (/products, pending) + Book a Demo (/contact)
lib/productsData.ts             Unified multi-category product catalog (21 products across all
                                 6 categories) — single source for the upcoming /products master
                                 grid, category pages, and product detail pages. Templates keep
                                 the 3-tier (Managed/Self-Hosted/Custom) pricing shape; every
                                 other category is a flat startingPrice.
lib/categoryIcons.tsx            Shared category iconName -> LucideIcon map (new components only —
                                 CategoryShowcase.tsx/FeaturedProducts.tsx keep their own inline copies)
components/shared/SectionHeader.tsx  Reusable eyebrow+title+subtitle section header, fade+slide entrance
components/home/ProductCard.tsx      Reusable product tile driven by the Product type (productsData.ts)
components/home/ComparisonTable.tsx  Reusable tier comparison table, mobile horizontal scroll, count-up price
components/home/FeatureGrid.tsx      Reusable 2x3/1-col benefits grid, staggered fade-in
components/home/VideoCarousel.tsx    Reusable video demo carousel, 60%-visibility autoplay, swipe support
lib/templatesSectionData.ts     Templates section content: benefits, why-us copy, demo video
components/home/TemplatesSection.tsx  Homepage Templates section (Phase 2, 1/6) — comparison table,
                                 benefits grid, why-us, demo, product cards. Not wired into page.tsx yet
lib/tshirtsSectionData.ts       T-Shirts section content: gallery slides, story quote
components/home/TShirtsSection.tsx  Homepage T-Shirts section (Phase 2, 2/6) — design gallery carousel,
                                 story callout, product cards. Not wired into page.tsx yet
lib/aiVideosSectionData.ts      AI Videos section content: sample videos, custom-order callout
components/home/AIVideosSection.tsx  Homepage AI Videos section (Phase 2, 3/6) — video carousel,
                                 custom callout, product cards. Not wired into page.tsx yet
lib/fileToolsSectionData.ts     File Tools section content: 3-item feature grid
components/home/FileToolsSection.tsx  Homepage File Tools section (Phase 2, 4/6) — feature grid,
                                 product cards. Lightest section, no new CSS. Not wired into page.tsx yet
services/supabase.ts            browserClient (anon) + adminClient (service role)

NOTE: MARKETPLACE_IMPROVEMENTS.md (repo root) is the multi-category
marketplace build plan (Templates, T-Shirts, AI Videos, File Tools,
Tutorials, Game Characters). The site is mid-pivot: the homepage is
now fully rebuilt per the plan's 10-section spec (Hero, Quick Wins,
Category Showcase, Featured Products, How It Works, Testimonials,
FAQ, CTA Banner), but /shop, /portfolio, /features, /how-it-works,
/contact, and /testimonials are still the older single-template
(resort booking) pages — not yet split into /products + 6 category
pages per
that plan's Site Map (Section 3).

components/products/ProductDetail.tsx       Shared product detail body (icon,
                                              rating, price, variant selector,
                                              "More in this category" strip)
app/styles/productDetail.css                 Product detail page styles
app/(public)/templates/[slug]/page.tsx       Templates product detail (/templates/[slug])
app/(public)/templates/[slug]/not-found.tsx  Invalid-slug 404 state
app/(public)/tshirts/[slug]/page.tsx         T-Shirts product detail (/tshirts/[slug])
app/(public)/tshirts/[slug]/not-found.tsx    Invalid-slug 404 state
app/(public)/ai-videos/[slug]/page.tsx       AI Videos product detail (/ai-videos/[slug])
app/(public)/ai-videos/[slug]/not-found.tsx  Invalid-slug 404 state
app/(public)/file-tools/[slug]/page.tsx      File Tools product detail (/file-tools/[slug])
app/(public)/file-tools/[slug]/not-found.tsx Invalid-slug 404 state
app/(public)/tutorials/[slug]/page.tsx       Tutorials product detail (/tutorials/[slug])
app/(public)/tutorials/[slug]/not-found.tsx  Invalid-slug 404 state
app/(public)/game-characters/[slug]/page.tsx      Game Characters product detail
app/(public)/game-characters/[slug]/not-found.tsx Invalid-slug 404 state
lib/legalData.ts                             Static content for all 4 legal pages
components/legal/LegalDocument.tsx           Shared legal page body (title, sections)
app/styles/legal.css                         Legal page styles
app/(public)/privacy/page.tsx                Privacy Policy (/privacy)
app/(public)/terms/page.tsx                  Terms of Service (/terms)
app/(public)/security/page.tsx               Security (/security)
app/(public)/refund-policy/page.tsx          Refund Policy (/refund-policy)

5. BUILD PLAN — REMAINING PAGES (in order)
--------------------------------------------------------
[DONE] 1. Home/Landing
        - Hero section, CTAs ("Browse Templates", "See Case Studies")
        - Quick wins: client wordmarks + result stats

[DONE] 2. Portfolio/Showcase (/portfolio)
        - Gallery grid of past resort website projects
        - Each card -> case study page showing:
          client name, resort type, problem, solution,
          results, live demo link

[DONE] 3. Template Shop (/shop)
        - 3 pricing tiers: Managed SaaS (monthly), Self-hosted
          (one-time), Custom build (retainer)
        - Each tier: price, feature list, "Get Started" button

[DONE] 4. Why Choose This Template (/features)
        - Tech stack breakdown
        - What's included out of the box
        - ROI calculator (e.g. hours saved per week)
        - Comparison table vs. competitors / building from scratch

[DONE] 5. How It Works (/how-it-works)
        - Step-by-step onboarding flow per tier
          (e.g. Tier 1: Sign up -> customize branding -> launch in 48h)

[DONE] 6. Testimonials (/testimonials)
        - Resort owner quotes, ease-of-use / results focus

[DONE] 7. Blog (/blog) — repurposed from marketing guides
        - Programming tutorials the site owner writes and pastes
          into lib/blogData.ts, plus social media links

[DONE] 8. Contact / Get Demo (/contact)
        - Contact form, calendar link for a walkthrough call

[ ] 9. superAdmin dashboard
        - Login page (app/(auth)/login)
        - Manage portfolio entries, pricing tiers, testimonials
        - middleware.ts auth guard for /superAdmin/*

5B. MARKETPLACE PIVOT — additional pages/sections from
    MARKETPLACE_IMPROVEMENTS.md, tracked separately since they
    layer on top of (not replace) the plan above.
--------------------------------------------------------
Homepage sections (10 total per the plan):
[DONE] Hero Section
[DONE] Quick Wins / Trust Metrics
[DONE] Category Showcase Grid (6 cards)
[DONE] Featured Products Carousel (auto-scroll, pause on hover, arrows)
[DONE] How It Works (universal 3-step: Browse/Choose/Start)
[DONE] Testimonials Carousel (homepage section — distinct from the
       existing static /testimonials page grid)
[DONE] FAQ Accordion
[DONE] CTA Banner (bottom of homepage)

Site structure (not yet started):
[ ] /products master grid (filter/sort/search)
[ ] 6 category pages: /templates, /tshirts, /ai-videos,
    /file-tools, /tutorials, /game-characters
[ ] Product detail pages per category ([slug], variant selectors)
[DONE] lib/productsData.ts — unified multi-category product schema
    (Section 5 of the plan); categoryShowcaseData.ts and
    featuredProductsData.ts are still separate placeholder files
[ ] /pricing (dedicated marketplace pricing — /shop is still the
    old single-template 3-tier page)
[ ] /compare (variant comparison tool)
[ ] /case-studies (rebrand of /portfolio with category tags)
[ ] /faq, /about
[ ] /security, /privacy, /terms, /refund-policy

6. HOW WE'RE WORKING
--------------------------------------------------------
One page/feature is built per turn, delivered as a ZIP (or
individual files if small), with a summary + verification
steps. After each one, I ask before starting the next so
nothing gets scaffolded ahead of what's actually requested.

7. ENVIRONMENT VARIABLES REQUIRED
--------------------------------------------------------
.env.local:
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  SUPABASE_SERVICE_ROLE_KEY=

8. CHANGE LOG
--------------------------------------------------------
Every create/update/delete/modify on this project gets a
one-line summary here, newest at the bottom. When this file
passes 1000 lines, it closes and logging continues in
overviewProject-1.txt (then -2.txt, and so on) until the
project is done.

[2026-08-05] Home/Landing page built: hero section (headline,
  CTAs to /shop and /portfolio, decorative product mockup),
  quick-wins strip (client wordmarks + result stats), shared
  NavBar (with mobile menu) and Footer moved into a new
  (public) route group, mediaQueries.css breakpoints added,
  @supabase/supabase-js installed.

[2026-08-05] Portfolio/Showcase built: /portfolio grid (4
  placeholder case studies matching the Home page wordmarks)
  and /portfolio/[slug] case study detail pages (problem,
  solution, results, live demo link), with a not-found state
  for invalid slugs. Fixed two pre-existing bugs found while
  building this: app/globals.css never defined the design
  token system (--color-accent, --space-*, --z-*, --transition-*)
  that home.css/shared.css already depended on, so it's been
  added properly; and app/layout.tsx imported "./styles/globals.css"
  while the file lived at app/globals.css — moved the file into
  app/styles/ to match the import and the project's own CSS
  convention. Also fixed an invalid LayoutProps<"/"> type in
  layout.tsx that failed npx tsc --noEmit.

[2026-08-05] Template Shop built: /shop page with 3 pricing
  tier cards (Managed SaaS, Self-Hosted, Custom Build), tier
  names matching the tags already used on Portfolio case
  studies for consistency. Middle tier (Self-Hosted) marked
  Recommended with an accent border. Each CTA links to
  /contact?tier=[slug] (page not yet built — item 8). Note
  strip below the grid links back to /portfolio.

[2026-08-22] Why Choose This Template built: /features page with a
  tech stack grid, an out-of-the-box included-features grid, an
  interactive ROI calculator (sliders for manual admin hours/week and
  hourly rate, computes hours + money saved per month, assumes 80%
  automation), and a comparison table (this template vs. building
  from scratch vs. a generic site builder). Note strip links to
  /shop. Fixed a pre-existing bug found while building this: .eyebrow,
  .heroTitle, .heroSubtitle, .buttonPrimary, and .buttonSecondary
  were only defined in home.css, which nothing but the Home page
  imports — so a direct/hard load of /shop, /portfolio, or the
  portfolio 404 state rendered those elements unstyled. Moved all
  five classes into shared.css, which every public page loads via
  app/(public)/layout.tsx, and removed the now-duplicate definitions
  from home.css.

[2026-08-22] How It Works built: /how-it-works page with a tabbed
  OnboardingFlow (Client Component) — one tab per pricing tier
  (Managed SaaS, Self-Hosted, Custom Build), each showing its own
  numbered step timeline with per-step duration labels and a total
  timeline callout. Defaults the active tab to ?tier=[slug] so a
  visitor clicking through from a /shop CTA lands on the matching
  flow. Note strip links back to /shop.

[2026-08-22] Testimonials built: /testimonials page with a
  TestimonialGrid quote-card grid, sourcing all 4 quotes from the
  same clients already shown on /portfolio (Cabana Bay Resort, Azure
  Point, Marlin Cove, Solstice Villas) so each card links back to
  that client's full case study. Each card shows the quote, person
  name/role, and a tier tag matching the client's portfolio tags.
  Note strip links back to /portfolio.

[2026-08-22] Blog built, repurposing item 7 from generic marketing
  guides into a programming-tutorial blog: /blog index (BlogGrid) and
  /blog/[slug] tutorial pages (TutorialDetail), with a not-found state
  for invalid slugs. lib/blogData.ts holds placeholder tutorial
  content as heading/paragraph/code blocks — paste new tutorials there
  to publish them. Added lib/socialLinksData.ts + shared SocialLinks
  icon-row component (hand-drawn SVG brand icons, since lucide-react
  dropped brand/logo icons from its set), rendered in both the Blog
  header and the site Footer. Footer's Company column now links to
  /blog.

[2026-08-22] Contact/Get Demo built: /contact page with a ContactForm
  (name, email, resort name, tier select, message), posting to a new
  /api/contact route that validates server-side and returns the
  standard success/error JSON shape — no email/DB delivery wired yet,
  logged server-side as a placeholder pending EmailJS or a Supabase
  table. Tier select pre-fills from ?tier=[slug], matching the CTAs
  already in place on /shop and NavBar. Added a calendar-booking
  callout block above the form (CALENDAR_BOOKING_URL in
  lib/contactData.ts — placeholder link, swap for a real Calendly/
  Cal.com URL). Wrapped ContactForm in <Suspense> since it reads
  useSearchParams. No other files needed changes — /shop's CTAs and
  the NavBar's Contact link/CTA already pointed at /contact.

[2026-08-27] (backfill — these two were built but never logged here)
  Category Showcase built: homepage section with 6 marketplace
  category cards (Templates, T-Shirts, AI Videos, File Tools,
  Tutorials, Game Characters) per MARKETPLACE_IMPROVEMENTS.md.
  Each card links to /shop?category=[slug] since dedicated category
  routes don't exist yet.

[2026-08-27] (backfill) Featured Products built: "Bestsellers This
  Month" homepage carousel — auto-scrolls horizontally on tablet/
  desktop (pauses on hover or arrow use), renders as a vertical
  stack on mobile. No real product photos yet, so cards show a
  category-tinted icon placeholder.

[2026-08-28] Overview corrected: file map and build plan didn't
  reflect the two entries above, or the fact that this project is
  mid-pivot from a single-template resort site to a multi-category
  marketplace per MARKETPLACE_IMPROVEMENTS.md. Added a new Section
  5B tracking the marketplace pivot's homepage sections and site
  structure separately from the original single-template build plan
  (Section 5), so "DONE" status can be trusted again without
  cross-checking git log.

[2026-08-28] How It Works (homepage) built: universal 3-step section
  (Browse Products → Choose What Fits → Start Using It) added to the
  homepage after Featured Products. Kept as a new component
  (HowItWorksSection.tsx) separate from the existing per-tier
  OnboardingFlow.tsx on /how-it-works, since that page still covers
  the Template product's own signup flow and stays as-is — this
  section is the marketplace-wide explainer for any category. Icons
  scale+rotate in on scroll, text slides in from the left, and a
  connecting line animates its width between steps (row layout from
  tablet up, stacked column on mobile). "View All Products" CTA
  links to /shop pending /products. npx tsc --noEmit passes clean.

[2026-09-01] Testimonials Carousel (homepage) built: "What Our
  Creators Say" section added after How It Works. Six quotes, one per
  marketplace category (Templates/T-Shirts/AI Videos/File
  Tools/Tutorials/Game Characters), each card showing a star rating,
  italic quote, initials avatar, author name/role, and a category
  badge. Auto-scrolls right-to-left on an 18s infinite loop (opposite
  direction from Featured Products' 20s left-to-right loop), pauses
  on hover, prev/next arrows fade in on carousel hover, falls back to
  a plain vertical stack under 768px — same responsive pattern as
  FeaturedProducts.tsx. New file lib/homeTestimonialsData.ts keeps
  this separate from lib/testimonialsData.ts (the resort-owner quotes
  on the standalone /testimonials page). npx tsc --noEmit passes
  clean.

[2026-09-01] FAQ Accordion (homepage) built: "Frequently Asked
  Questions" section added after Testimonials. Six general
  marketplace questions (payment methods, refunds, access time,
  support, commercial-use licensing, catalog cadence), single-open
  accordion (opening one item closes whichever was previously open),
  chevron rotates 180deg on expand, answer panel animates
  height/opacity via framer-motion AnimatePresence, proper
  aria-expanded/aria-controls on the toggle button and role="region"
  on the answer panel. "See All FAQs" CTA links to /faq (page itself
  still pending, Phase 3 of the marketplace plan — same forward-
  linking pattern already used elsewhere on the homepage). npx tsc
  --noEmit passes clean.

[2026-09-01] CTA Banner (homepage) built: "Ready to Get Started?"
  section added as the final homepage section, after FAQ. Gradient
  card (surface-active to surface), centered heading + subtext, two
  CTAs — "Browse All Products" (/products, pending — same forward-
  linking pattern used elsewhere) and "Book a Demo" (/contact,
  already live). Container fades in at 80% viewport visibility, text
  slides up + fades in, buttons scale in with a 0.2s delay via a
  cubic-bezier ease; primary button scales up on hover, secondary
  button's border/text shift to accent color on hover. Heading steps
  from 28px (mobile) to 40px (desktop, mediaQueries.css). This
  completes all 10 homepage sections from MARKETPLACE_IMPROVEMENTS.md
  Section 1 — homepage rebuild is DONE. npx tsc --noEmit passes
  clean.

[2026-09-01] lib/productsData.ts built: unified Product interface and
  21-item catalog spanning all 6 marketplace categories (3-4 products
  each), per Section 5 of MARKETPLACE_IMPROVEMENTS.md. price.starting
  Price is the universal sort/filter value; the Managed/Self-Hosted/
  Custom tier breakdown from the plan's example only applies to
  Templates, matching that category's real pricing model (others are
  flat one-time/subscription prices). Each product also carries
  dateAdded and trendingScore to drive the upcoming /products page's
  "Newest" and "Trending" sort options. iconName reuses the same
  category-tinted placeholder pattern as FeaturedProducts.tsx — no
  real product photos yet. This is the data layer only; /products
  master grid, the 6 category pages, and product detail pages are
  still pending. npx tsc --noEmit passes clean.

[2026-09-01] Phase 1 reusable components built, per IMPROVEMENTS.md
  Section 11/12 rollout plan: components/shared/SectionHeader.tsx
  (eyebrow+title+subtitle header, fade+slide entrance), components/
  home/ProductCard.tsx (reads the shared Product type from
  lib/productsData.ts), components/home/ComparisonTable.tsx (generic
  tier comparison table, horizontally scrollable on mobile, optional
  count-up price animation), components/home/FeatureGrid.tsx (2x3
  desktop / 1-col mobile benefits grid, staggered fade-in), and
  components/home/VideoCarousel.tsx (single-video-at-a-time demo
  carousel, native <video> controls, plays only at 60%+ viewport
  visibility via IntersectionObserver, swipe support on touch). New
  lib/categoryIcons.tsx centralizes the category icon map so these
  new components don't add a third inline copy of it (the two
  existing inline copies in CategoryShowcase.tsx/FeaturedProducts.tsx
  are untouched). New shared.css classes: .sectionHeader/Left,
  .srOnly, .productCard*, .comparisonTable*, .featureGrid*,
  .videoCarousel*; responsive rules added to mediaQueries.css per
  Rule 9. None of these components are wired into the homepage yet —
  that's Phase 2 (the six category sections) and Phase 3
  (integration). npx tsc --noEmit passes clean.

[2026-09-01] Phase 2 (1/6): TemplatesSection.tsx built — comparison
  table (Managed/Self-Hosted/Custom, reusing PRICING_TIERS from
  pricingData.ts; feature rows are the deduped union of each tier's
  own bullets rather than a separately invented master list), 6-item
  benefits grid, 3-column "Why Us" trust builder (new .whyUsGrid/Item/
  Heading/Body classes in shared.css, row layout from tablet up),
  demo VideoCarousel, and product cards for the "templates" category
  (productsData.ts). New lib/templatesSectionData.ts holds the
  section's own content (benefits, why-us copy, demo video) that
  doesn't live anywhere else. New shared.css classes reused by every
  future category section: .categorySection, .sectionContainer,
  .sectionContent, .productCardsGrid, .sectionCTA. Not wired into
  page.tsx yet — still Phase 2/3. npx tsc --noEmit passes clean.

[2026-09-01] Phase 2 (2/6): TShirtsSection.tsx built — manually-
  controlled Design Gallery Carousel (3 slides from the "tshirts"
  product names, tinted gradient placeholders since no real photos
  exist yet, overlay name+tagline bottom-right, arrow nav, dots,
  narrows to 60% width from tablet up, subtle scroll-linked parallax
  on the active image via framer-motion's useScroll/useTransform),
  "The Story" quote callout (accent left-border, italic quote +
  attribution), product cards for the "tshirts" category, and CTA.
  New lib/tshirtsSectionData.ts holds the gallery slide copy and
  story quote. New shared.css classes: .designGallery*, .storyCallout*.
  Not wired into page.tsx yet — still Phase 2/3. npx tsc --noEmit
  passes clean.

[2026-09-01] Phase 2 (3/6): AIVideosSection.tsx built — reuses the
  Phase 1 VideoCarousel component (its original spec target) with 3
  sample videos, a "Need custom videos?" callout (2-col from tablet
  up: text left, CTA right; accent left-border, bounce-style scale-in
  entrance), product cards for the "ai-videos" category, and CTA. New
  lib/aiVideosSectionData.ts holds the sample video list and callout
  copy. New shared.css classes: .customCallout*. Not wired into
  page.tsx yet — still Phase 2/3. npx tsc --noEmit passes clean.

[2026-09-01] Phase 2 (4/6): FileToolsSection.tsx built — the lightest
  of the six sections per spec: header, the reusable FeatureGrid (3
  items instead of Templates' 6), product cards for the "file-tools"
  category, CTA. No new reusable components or CSS needed — entirely
  built from Phase 1 pieces + existing section-anatomy classes. New
  lib/fileToolsSectionData.ts holds the 3 feature items. Note: spec's
  "icon colors rotate through green/teal/coral" was intentionally not
  followed — kept the single accent-green icon color instead, per
  Rule 17.2 (never more than one accent color per project); documented
  inline in the data file. Not wired into page.tsx yet — still Phase
  2/3. npx tsc --noEmit passes clean.
========================================================
[2026-09-01] Phase 2 (5/6): TutorialsSection.tsx built — 3-persona
  "Who's this for?" callout (Developers/Designers/Founders; icon +
  role + bullet, fade-in stagger 0.15s, no hover state per spec;
  role-based background uses single-accent opacity steps instead of
  a second hue, per Rule 17.2), product cards for the "tutorials"
  category ordered Beginner→Intermediate→Advanced with a level badge
  (green/amber/red status tokens) and duration overlaid per card via
  a thin wrapper — ProductCard.tsx itself untouched, level/duration
  come from new lib/tutorialsSectionData.ts (TUTORIAL_META, keyed by
  product id) rather than extending the shared Product type. New
  shared.css classes: .personaGrid/Item/Icon/Role/Bullet,
  .tutorialCardWrap/LevelBadge/Duration + level color variants. Not
  wired into page.tsx yet — still Phase 2/3. npx tsc --noEmit passes
  clean.
========================================================
[2026-09-02] Fixed homepage marketplace-alignment mismatch per
  improvement_1.md Section 2/3: QuickWins.tsx's proof section was
  still resort-template-only (label "Trusted by resorts already
  running the template", resort wordmarks Cabana Bay/Azure
  Point/Marlin Cove/Solstice Villas, resort-booking stats +38%
  bookings/5hrs saved/48h launch) despite the rest of the homepage
  already selling all 6 marketplace categories. Replaced with
  marketplace-wide content: label now "One marketplace, every
  category creators need", wordmark row now lists the 6 categories
  (Templates/T-Shirts/AI Videos/File Tools/Tutorials/Game
  Characters), stats now "18+ Products across 6 categories", "6
  Creator categories in one marketplace", "48h Average delivery
  time" (18 verified by counting lib/productsData.ts product
  objects). Also repointed NavBar.tsx: "Shop" nav link and both
  "Browse Marketplace" CTAs (desktop + mobile) were still pointing
  to /shop (old single-template pricing page) — now point to
  /products (the marketplace master grid, already built per
  improvement_1.md Section 4). Nav label renamed "Shop" → "Products"
  to match. npx tsc --noEmit passes clean.
========================================================
[2026-09-02] Reordered app/(public)/page.tsx homepage section render
  order. HowItWorksSection moved from position 11 (after all 6
  category sections) to position 4 (right after CategoryShowcase,
  before FeaturedProducts) — rationale: visitors should understand
  the browse/choose/start process before scrolling through 6 long
  category-specific sections, not after. New order: HeroSection,
  QuickWins, CategoryShowcase, HowItWorksSection, FeaturedProducts,
  TemplatesSection, TShirtsSection, AIVideosSection, FileToolsSection,
  TutorialsSection, GameCharactersSection, TestimonialsSection,
  FAQAccordion, CTABanner. No component files touched, only render
  order in page.tsx — confirmed NavBar's /#how-it-works-section and
  /#testimonials-section scroll-anchor links still resolve correctly
  since the id attributes live on the section elements themselves,
  order-independent. npx tsc --noEmit passes clean.
========================================================
[2026-09-02] Implemented improvement_1.md Section 5 build-order item 4
  (first half): retired /shop, built marketplace-wide /pricing.
  - New app/(public)/pricing/page.tsx: 6-category pricing overview
    reusing lib/categoryShowcaseData.ts's CATEGORY_SHOWCASE (same
    data already shown on the homepage's CategoryShowcase section, so
    starting prices never contradict each other). Each card links to
    its own category page. Closing note bridges to /features for
    visitors who need the deeper Templates-only Managed/Self-Hosted/
    Custom tier comparison (that 3-tier model stays there — it's
    genuinely Templates-specific, not marketplace-wide, so it was not
    duplicated onto /pricing). New app/styles/pricing.css added.
  - app/(public)/shop/page.tsx replaced with a redirect("/pricing")
    (old single-template Managed/Self-Hosted/Custom pricing page) so
    old bookmarks/links funnel forward instead of 404ing.
  - Fixed 8 stale "/shop" link references discovered during this pass
    (pre-dated /products and the 6 category pages existing, so they
    were pointing at the wrong destination even before /shop's
    retirement): HeroSection.tsx "Browse All Products"/"See What's
    New" → /products and /products?sort=newest; TemplatesSection.tsx
    "Explore Templates" → /templates; CategoryShowcase.tsx's 6 card
    links (was /shop?category=X, a query param /shop never actually
    read) → /${slug} (their real category pages); features/page.tsx
    and how-it-works/page.tsx "see pricing" CTAs → /pricing;
    CaseStudyDetail.tsx "Browse Templates" → /templates;
    HowItWorksSection.tsx (homepage) "View All Products" → /products;
    Footer.tsx "Shop" link → "Pricing" pointing at /pricing, added a
    "Products" link pointing at /products.
  - lib/pricingData.ts (PRICING_TIERS) was NOT touched/removed — it's
    still legitimately used by TemplatesSection.tsx's own Templates-
    specific 3-tier comparison table on the homepage.
  - components/shop/PricingGrid.tsx and app/styles/shop.css are now
    orphaned (no longer imported anywhere) — flagged for cleanup, not
    deleted automatically.
  - improvement_1.md Section 3's "/shop" row and Section 4's "/pricing"
    checkbox marked done.
  - Portfolio → /case-studies rebrand (second half of build-order item
    4) intentionally deferred to its own turn.
  npx tsc --noEmit passes clean.
========================================================
[2026-09-02] Implemented improvement_1.md Section 5 build-order item 4
  (second half): rebranded /portfolio to /case-studies.
  - New app/(public)/case-studies/page.tsx and
    app/(public)/case-studies/[slug]/page.tsx +
    app/(public)/case-studies/[slug]/not-found.tsx (moved from
    /portfolio, copy updated: "Resorts already running on the
    template" → "Real projects, real results"; header note now
    mentions more categories will join Templates here).
  - app/(public)/portfolio/page.tsx and
    app/(public)/portfolio/[slug]/page.tsx replaced with
    redirect()s to /case-studies and /case-studies/[slug] (slug
    preserved) so old bookmarks/deep links don't 404.
  - lib/portfolioData.ts: added `category`/`categoryLabel` fields to
    PortfolioProject (all 4 existing projects are genuinely
    "templates" — not a placeholder value) so case studies are ready
    to filter/tag by marketplace category once other categories have
    their own. Kept the file name and PortfolioProject/
    PORTFOLIO_PROJECTS/getPortfolioProjectBySlug identifiers as-is
    (not renamed to caseStudiesData.ts) — only the user-facing route
    changed, renaming the file would have forced needless import
    churn across every consumer for no functional benefit.
  - components/portfolio/PortfolioGrid.tsx: card links repointed to
    /case-studies/[slug]; added a category badge (categoryLabel) in
    the top-left of each card's cover — the "category tags" piece
    from improvement_1.md Section 3. New CSS: .portfolioCardCategoryBadge
    in app/styles/portfolio.css (portfolioCardCover given position:
    relative to anchor it).
  - components/portfolio/CaseStudyDetail.tsx: back-link repointed to
    /case-studies.
  - Fixed dependent stale "/portfolio" link references: Footer.tsx
    ("Portfolio" → "Case Studies", now points at /case-studies);
    TestimonialGrid.tsx card links; /testimonials page's "View Case
    Studies" CTA and its "in our Portfolio" copy reference (fixed
    since it named the old page directly — the rest of that page's
    resort-only copy is intentionally left for its own Section 3 turn,
    not rewritten here).
  - improvement_1.md Section 3's "/portfolio" row marked done.
  npx tsc --noEmit passes clean.

[2026-09-02] Built /compare — marketplace-wide product comparison
  tool (improvement_1.md Section 4, "/compare — variant comparison
  tool"). New files: app/(public)/compare/page.tsx (Server Component
  shell, reads PRODUCTS), components/compare/ProductCompareTool.tsx
  (Client Component — 3 search-to-add slots, excludes already-picked
  products, remove button per filled slot; renders a comparison table
  once 2+ products are picked: category, rating, starting price,
  badge, description, and a Variants row that only appears when at
  least one selected product has variants, listing each Templates
  variant's name and monthly price), app/styles/compare.css. Modified
  app/styles/mediaQueries.css: added the tablet-up breakpoint that
  puts the 3 slots in a row (stacked on mobile, per Rule 9 — no
  breakpoints inside compare.css itself). Also confirmed while
  reviewing improvement_1.md's Section 1 (homepage metadata), Section
  2 (Footer), and Section 5 (/features, /how-it-works, /testimonials,
  /contact copy) that all of those were already implemented in a
  prior turn but never had their checkboxes/table rows updated in the
  doc — no code changes were needed for those, only this /compare
  build. npx tsc --noEmit passes clean.

[2026-09-02] Closed out improvement_1.md Section 4's last two missing
  pages. app/(public)/faq/page.tsx: new redirect-only route to
  /support, same pattern as app/(public)/shop/page.tsx -> /pricing -
  /support already ships a searchable, category-grouped FAQ
  (SupportFaqAccordion) and NavBar already links "Support" there, so
  a second standalone /faq page would have forked the same content in
  two places instead of adding real value. app/(public)/about/page.tsx
  (new): mission intro, a category grid reusing CATEGORY_SHOWCASE
  (lib/categoryShowcaseData.ts - same source of truth as the
  homepage, so it can't drift), and 3 value-pillar cards from the new
  lib/aboutData.ts (static placeholder content - no real founder/team
  bios exist in the codebase yet, so this deliberately stays generic
  rather than inventing names or a founding story). New
  app/styles/about.css. Added "About" to Footer's Company column
  (components/shared/Footer.tsx) so the new page isn't an orphan with
  no inbound link. improvement_1.md Section 4 is now fully checked
  off - every page in that list exists. npx tsc --noEmit and eslint
  both pass clean on all new/modified files.

[2026-09-02] Closed out improvement_1.md Section 4's last two open
  items: product detail pages and the 4 legal pages. New shared
  components/products/ProductDetail.tsx (icon thumb, rating,
  description, variant selector for Templates' Static/Dynamic/Modern
  breakdown, price, CTA row, "More in this category" strip) plus
  app/styles/productDetail.css - reused by 6 new [slug] routes
  (app/(public)/{templates,tshirts,ai-videos,file-tools,tutorials,
  game-characters}/[slug]/page.tsx), each with generateStaticParams
  scoped to its own category, generateMetadata, and a sibling
  not-found.tsx (same pattern as case-studies/[slug]). Repointed the
  stale /shop?category= "View Details" links in
  components/home/ProductCard.tsx and components/home/
  FeaturedProducts.tsx to the new real detail pages; also removed a
  pre-existing duplicated file-header comment block found while
  editing FeaturedProducts.tsx. New lib/legalData.ts (Privacy Policy,
  Terms of Service, Security, Refund Policy content) plus shared
  components/legal/LegalDocument.tsx and app/styles/legal.css, reused
  by 4 new pages: /privacy, /terms, /security, /refund-policy. Added
  a "Legal" column to Footer.tsx linking all 4 so none of them are
  orphaned with no inbound link. improvement_1.md Section 4 is now
  fully checked off - every page in that list exists. npx tsc
  --noEmit passes clean on all new/modified files.

[2026-09-02] Built the forgot-password / reset-password flow flagged
  as missing from login_and_registration_page.md Section 3.1. New
  pages app/auth/forgot-password/page.tsx (email-only form) and
  app/auth/reset-password/page.tsx (new password form), backed by
  components/auth/ForgotPasswordForm.tsx and ResetPasswordForm.tsx.
  New API routes: app/api/auth/forgot-password/route.ts (rate-limited
  3/15min, calls Supabase resetPasswordForEmail, always returns the
  same generic message so emails can't be enumerated, logs
  password_reset_requested) and app/api/auth/reset-password/route.ts
  (logging-only - the actual password update runs client-side via
  supabaseBrowserClient.auth.updateUser() using the temporary recovery
  session Supabase's SDK attaches from the email link; this route just
  records password_reset_completed to SecurityLog). ResetPasswordForm
  verifies that recovery session on mount and shows an "invalid or
  expired link" state instead of a form if it's missing. Also fixed a
  pre-existing bug found while wiring this up: app/(auth)/ was a
  parenthesized route group, which Next.js strips from the URL, so the
  login page was actually served at /login while every reference in
  middleware.ts, SignInForm, and BuyerNav pointed at /auth/login - a
  404 in production. Renamed the folder to app/auth/ (a real segment)
  so /auth/login, /auth/forgot-password, and /auth/reset-password all
  resolve as the rest of the app already assumes. Extended
  app/styles/auth.css with .authPageHeading/.authPageDescription (for
  the two new tab-less pages) and an anchor-tag variant of
  .authSwitchPrompt's link styling. No schema change - reuses the
  existing SecurityLog and RateLimitAttempt models with new eventType/
  endpoint string values.

[2026-09-02] Wired the debounced email-uniqueness check into
  RegisterForm.tsx (previously built but unused) - Section 3.2 of
  login_and_registration_page.md. On email change, a 500ms debounce
  (useEffect + setTimeout, guarded by an incrementing request-id ref so
  a stale response can never overwrite a newer one) calls
  GET /api/auth/check-email and shows an inline "Checking
  availability..." hint, then "Email already in use." if taken -
  never a toast, matching Section 9. validate() now also blocks submit
  when the check has already resolved to "taken". Added
  .authFieldHint to app/styles/auth.css for the quiet (non-error)
  checking state. Also added rate limiting (100/15min, general API
  tier) to app/api/auth/check-email/route.ts as a backstop, since it's
  now called on every debounce cycle instead of once per submit -
  reuses the existing lib/rateLimit.ts helper, same pattern as
  login/register/forgot-password.

[2026-09-02] Added the Section 10 post-registration onboarding modal
  (login_and_registration_page.md gap) - new components/buyer/
  OnboardingModal.tsx client component renders the "Welcome to
  Matthew Studio Marketplace!" overlay with the 4-item checklist and
  Get Started button. Triggered via a one-shot sessionStorage flag
  (mtwOnboarding:justRegistered) set by RegisterForm.tsx immediately
  before its router.push("/buyer/dashboard") call; OnboardingModal
  reads and clears the flag on mount so it never reappears on refresh
  or a later normal sign-in. No Settings page exists yet to host a
  permanent skip toggle, so dismissing the modal is the only skip
  path for now. Wired into app/buyer/dashboard/page.tsx; styles added
  at app/styles/onboardingModal.css, imported from app/buyer/layout.tsx.

[2026-09-02] Added CSRF double-submit cookie protection (Rule 32.2)
  across the entire auth flow - login_and_registration_page.md Section
  5 gap. New lib/csrf.ts (isomorphic - Web Crypto API, no Node-only
  imports, safe in Edge middleware and "use client" alike) exports
  generateCsrfToken/isValidCsrfRequest (server) and getCsrfHeader
  (client). middleware.ts now issues a non-HttpOnly csrf_token cookie
  (SameSite=strict, 4h) on every /auth/* and /buyer/* request that
  doesn't already have one - matcher widened from ["/buyer/:path*",
  "/auth/login"] to ["/buyer/:path*", "/auth/:path*"] so
  forgot-password and reset-password pages get the cookie too. All
  five state-changing auth endpoints (login, register, forgot-password,
  reset-password, logout) now call isValidCsrfRequest() first and
  return 403 on mismatch, before rate limiting runs. All five call
  sites (SignInForm, RegisterForm, ForgotPasswordForm,
  ResetPasswordForm, BuyerNav's Sign Out) now spread getCsrfHeader()
  into their fetch headers. No schema change.
