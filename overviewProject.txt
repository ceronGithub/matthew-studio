========================================================
MATTHEW STUDIO — PROJECT OVERVIEW & BUILD PLAN
========================================================

1. PROJECT NAME & PURPOSE
--------------------------------------------------------
Matthew Studio — a portfolio + shop site showcasing a resort
booking website template. Visitors browse past client work,
compare the 3 pricing tiers (managed SaaS / self-hosted /
custom build), learn what the template includes, read
testimonials, and book a demo. A single superAdmin account
manages the backend content (portfolio entries, pricing,
testimonials).

2. TECH STACK
--------------------------------------------------------
- Next.js 16.3.0 (App Router, Turbopack)
- React 19.2.8
- TypeScript
- Tailwind CSS v4 (+ custom design token system in
  app/styles/globals.css)
- framer-motion — section entrance animations
- lucide-react — icons
- axios — client-side HTTP calls
- Supabase — auth (superAdmin login) + database
- Fonts: Geist Sans / Geist Mono (next/font)

3. ACCOUNT TYPES
--------------------------------------------------------
- Public (no login) — all marketing/shop pages
- superAdmin — protected dashboard at /superAdmin,
  Supabase-authenticated, role checked via user_metadata.role

4. FILE MAP (as of this build)
--------------------------------------------------------
app/layout.tsx                  Root shell — fonts, global CSS only
app/styles/globals.css          Design tokens (color, spacing, z-index, transitions)
app/styles/shared.css           NavBar + Footer styles
app/styles/mediaQueries.css     All responsive breakpoints
app/(public)/layout.tsx         Public route group — renders NavBar + Footer
app/(public)/page.tsx           Home/Landing page
app/styles/home.css             Home page styles
app/api/auth/login/route.ts     superAdmin login endpoint (Supabase)
app/(auth)/                     Login page route (not yet built)
app/superAdmin/                 Admin dashboard (not yet built)
app/(public)/portfolio/page.tsx           Portfolio/Showcase grid (/portfolio)
app/(public)/portfolio/[slug]/page.tsx    Case study detail (/portfolio/[slug])
app/(public)/portfolio/[slug]/not-found.tsx  Invalid-slug 404 state
app/styles/portfolio.css        Portfolio grid + case study styles
components/portfolio/PortfolioGrid.tsx    Animated case study card grid
components/portfolio/CaseStudyDetail.tsx  Case study problem/solution/results layout
lib/portfolioData.ts            Static placeholder case study data (4 projects)
app/(public)/shop/page.tsx      Template Shop pricing tier grid (/shop)
app/styles/shop.css             Shop page + pricing card styles
components/shop/PricingGrid.tsx Animated pricing tier card grid
lib/pricingData.ts              Static placeholder pricing tier data (3 tiers)
app/(public)/features/page.tsx  Why Choose This Template page (/features)
app/styles/features.css         Tech stack, included-features, ROI, comparison table styles
components/features/ROICalculator.tsx  Client-side hours/money-saved calculator
lib/featuresData.ts             Static tech stack, included features, comparison rows
app/(public)/how-it-works/page.tsx  How It Works page (/how-it-works)
app/styles/how-it-works.css     Tab list + step timeline styles
components/how-it-works/OnboardingFlow.tsx  Client tier-tab switcher + step timeline
lib/howItWorksData.ts           Static per-tier onboarding step data
app/(public)/testimonials/page.tsx  Testimonials page (/testimonials)
app/styles/testimonials.css     Quote card grid styles
components/testimonials/TestimonialGrid.tsx  Animated testimonial quote card grid
lib/testimonialsData.ts         Static testimonial quotes (4 clients, matches portfolio)
components/shared/NavBar.tsx    Site nav, mobile menu
components/shared/Footer.tsx    Site footer
components/home/HeroSection.tsx Home hero + CTAs
components/home/QuickWins.tsx   Client wordmarks + result stats
services/supabase.ts            browserClient (anon) + adminClient (service role)

5. BUILD PLAN — REMAINING PAGES (in order)
--------------------------------------------------------
[DONE] 1. Home/Landing
        - Hero section, CTAs ("Browse Templates", "See Case Studies")
        - Quick wins: client wordmarks + result stats

[DONE] 2. Portfolio/Showcase (/portfolio)
        - Gallery grid of past resort website projects
        - Each card -> case study page showing:
          client name, resort type, problem, solution,
          results, live demo link

[DONE] 3. Template Shop (/shop)
        - 3 pricing tiers: Managed SaaS (monthly), Self-hosted
          (one-time), Custom build (retainer)
        - Each tier: price, feature list, "Get Started" button

[DONE] 4. Why Choose This Template (/features)
        - Tech stack breakdown
        - What's included out of the box
        - ROI calculator (e.g. hours saved per week)
        - Comparison table vs. competitors / building from scratch

[DONE] 5. How It Works (/how-it-works)
        - Step-by-step onboarding flow per tier
          (e.g. Tier 1: Sign up -> customize branding -> launch in 48h)

[DONE] 6. Testimonials (/testimonials)
        - Resort owner quotes, ease-of-use / results focus

[ ] 7. Blog/Guides (/blog) — optional, SEO value
        - Articles like "How to increase resort bookings"

[ ] 8. Contact / Get Demo (/contact)
        - Contact form, calendar link for a walkthrough call

[ ] 9. superAdmin dashboard
        - Login page (app/(auth)/login)
        - Manage portfolio entries, pricing tiers, testimonials
        - middleware.ts auth guard for /superAdmin/*

6. HOW WE'RE WORKING
--------------------------------------------------------
One page/feature is built per turn, delivered as a ZIP (or
individual files if small), with a summary + verification
steps. After each one, I ask before starting the next so
nothing gets scaffolded ahead of what's actually requested.

7. ENVIRONMENT VARIABLES REQUIRED
--------------------------------------------------------
.env.local:
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  SUPABASE_SERVICE_ROLE_KEY=

8. CHANGE LOG
--------------------------------------------------------
Every create/update/delete/modify on this project gets a
one-line summary here, newest at the bottom. When this file
passes 1000 lines, it closes and logging continues in
overviewProject-1.txt (then -2.txt, and so on) until the
project is done.

[2026-08-05] Home/Landing page built: hero section (headline,
  CTAs to /shop and /portfolio, decorative product mockup),
  quick-wins strip (client wordmarks + result stats), shared
  NavBar (with mobile menu) and Footer moved into a new
  (public) route group, mediaQueries.css breakpoints added,
  @supabase/supabase-js installed.

[2026-08-05] Portfolio/Showcase built: /portfolio grid (4
  placeholder case studies matching the Home page wordmarks)
  and /portfolio/[slug] case study detail pages (problem,
  solution, results, live demo link), with a not-found state
  for invalid slugs. Fixed two pre-existing bugs found while
  building this: app/globals.css never defined the design
  token system (--color-accent, --space-*, --z-*, --transition-*)
  that home.css/shared.css already depended on, so it's been
  added properly; and app/layout.tsx imported "./styles/globals.css"
  while the file lived at app/globals.css — moved the file into
  app/styles/ to match the import and the project's own CSS
  convention. Also fixed an invalid LayoutProps<"/"> type in
  layout.tsx that failed npx tsc --noEmit.

[2026-08-05] Template Shop built: /shop page with 3 pricing
  tier cards (Managed SaaS, Self-Hosted, Custom Build), tier
  names matching the tags already used on Portfolio case
  studies for consistency. Middle tier (Self-Hosted) marked
  Recommended with an accent border. Each CTA links to
  /contact?tier=[slug] (page not yet built — item 8). Note
  strip below the grid links back to /portfolio.

[2026-08-22] Why Choose This Template built: /features page with a
  tech stack grid, an out-of-the-box included-features grid, an
  interactive ROI calculator (sliders for manual admin hours/week and
  hourly rate, computes hours + money saved per month, assumes 80%
  automation), and a comparison table (this template vs. building
  from scratch vs. a generic site builder). Note strip links to
  /shop. Fixed a pre-existing bug found while building this: .eyebrow,
  .heroTitle, .heroSubtitle, .buttonPrimary, and .buttonSecondary
  were only defined in home.css, which nothing but the Home page
  imports — so a direct/hard load of /shop, /portfolio, or the
  portfolio 404 state rendered those elements unstyled. Moved all
  five classes into shared.css, which every public page loads via
  app/(public)/layout.tsx, and removed the now-duplicate definitions
  from home.css.

[2026-08-22] How It Works built: /how-it-works page with a tabbed
  OnboardingFlow (Client Component) — one tab per pricing tier
  (Managed SaaS, Self-Hosted, Custom Build), each showing its own
  numbered step timeline with per-step duration labels and a total
  timeline callout. Defaults the active tab to ?tier=[slug] so a
  visitor clicking through from a /shop CTA lands on the matching
  flow. Note strip links back to /shop.

[2026-08-22] Testimonials built: /testimonials page with a
  TestimonialGrid quote-card grid, sourcing all 4 quotes from the
  same clients already shown on /portfolio (Cabana Bay Resort, Azure
  Point, Marlin Cove, Solstice Villas) so each card links back to
  that client's full case study. Each card shows the quote, person
  name/role, and a tier tag matching the client's portfolio tags.
  Note strip links back to /portfolio.
========================================================